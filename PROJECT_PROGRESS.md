# Ghost-OS 当前项目概述

更新日期：2026-03-10  
分支：main（与 `origin/main` 同步）  
阶段：MVP 骨架（主链路可用，核心能力持续补齐）

## 项目状态

- `Ghost-OS` 当前已形成清晰的三层架构：Execution、Central、Perception，整体目标是构建一个可通过 AI 驱动的数字分身执行层。
- **Execution Layer**（`drivers/native`）已具备基础原子能力与脚本执行能力，可承接截图、输入模拟、文件处理、脚本执行等底层动作。
- **Central Layer**（`core/bridge`）是当前最成熟的部分，已打通 Agent、Session、Tool、Provider、配置管理、会话持久化、流式响应、`ask_human` 续跑与 Memory 基线。
- **Perception Layer**（`apps/web`、`apps/cli`、`apps/android`）均已有可用 MVP，Web Console、CLI 与 Android 客户端能够完成基础交互与联动。

## 当前基础能力

- 三层边界、消息总线契约、`trace_id` 追踪链路、会话结束语义与跨端 DTO 已基本统一。
- Native 执行链路已具备文件工具、脚本沙箱、persistent mode 与浏览器查询相关基础模块，可与 Bridge 形成稳定调用闭环。
- Bridge 主流程已经可用，支持 Provider 调用、Tool 路由、Session 管理、停止控制、SSE 事件流与配置文件持久化。
- Memory 已从基础读写扩展到自动召回、Graph Memory、Decision Memory 与 Truth Schema shadow write，为后续上下文增强与经验沉淀提供基础。

## 当前工程状态

- `2026-03-10`：Web Console 前端侧边栏（SessionSidebar）替换为极简黑白风格，支持折叠与本地搜索过滤；仅做 UI/样式与交互增强，不改动会话数据/接口与 Bridge 主链。相关布局列宽改为自适应，移动端断点下侧边栏强制全宽以保持堆叠体验。
- `2026-03-10`：将 session artifact 下载的 stored path 路径边界校验下沉到 `core/bridge/artifacts`：新增 `SessionArtifactStore.ResolveStoredPath` / `OpenStoredFile` 统一收口 `Clean + BaseDir` 边界检查与可选 symlink escape 检测；`core/bridge/app/transport_handlers_sessions.go` 下载入口改为复用该 API，并补充 `stored_file_test.go` 覆盖越界与 symlink 逃逸。
- `2026-03-10`：修复 `core/bridge/app` 两处关键稳定性/安全边界问题：其一是 `agent_runtime_factory` 在依赖装配阶段避免提前创建 `executionClient` 导致的资源泄露；其二是收紧 `transport` 配置读取为 fail-closed，当 `config.toml` 存在但读取/解析失败时直接返回错误（避免 API Token 认证意外关闭、BindAddr/CORS 等静默降级）。新增 `transport_config_env_test.go` 回归并通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(TransportConfigFromTomlConfig|TransportConfigParseErrorFailsClosed|ResolveBindAddr)'` 验证。
- `2026-03-10`：收拢 `core/bridge` 的流式事件边界：`agent.RunStream()` 现在自己发出完整生命周期事件 `run_started -> ... -> message -> done`，新增 `StreamLifecyclePayloadBuilder` 让 `app` 只补 `session_id/session_ended` 等上下文，不再在 `service_usecase_agent` / `service_usecase_human` 里拼 `message/done/run_started`。同时在 `app` 增加 `streamTerminalBuffer`，确保 `message/done` 只会在会话持久化成功后对外放行，避免先发成功终态再报持久化错误；补充 `stream_terminal_buffer_test.go`，并更新 agent/transport/human/session-events 流式回归桩到新 contract。已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./agent`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(StreamTerminalBuffer|HandleAgentStream(ReturnsHeadersAndEvents|ValidationErrorEmitsErrorEvent)|HandleQuestionAnswerStreamReturnsEvents|SessionEventStreamReceivesAgentStreamProgress)'` 验证；另行尝试 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app`，仍被当前工作树中既有 `TestSessionTurnPreparerInjectAutoRecallPrioritizesHygieneRules` 与 `TestBusMemoryQueryReturnsGraphHits` 失败阻塞。
- `2026-03-10`：按“配置按子系统收口”的方向重构 `core/bridge/app` 运行时配置：`Config` 不再平铺 provider/memory/rss/worker/tool selector 数十个字段，改为组合 `ProviderConfig`、`MemoryRuntimeConfig`、`RSSConfig`、`WorkerConfig`、`ToolSelectorConfig` 五个子块；`loadConfigWithRuntime(...)` 同步改为按子块装配，`agent_runtime_factory`、`memory_runtime`、`rss_*`、`tool_selector`、`session_turn_preparer` 等消费点统一切到嵌套访问。顺手把明显只依赖单一子系统的签名收窄为 `ToolSelectorConfig`、`RSSConfig`、`ProviderConfig`，减少继续透传整份 `Config`。补充并改写相关配置 / selector / RSS / memory / session history 回归测试，已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(LoadConfigWithRuntime|InferProviderTypeDetectsCodexModel|ValidateRuntimeForExecutionRequiresAPIKeyForCodex|ParseFloatEnv_FallsBackOnInvalidValues|RuntimeConfigFromEnvPrefersMultiProviderConfigFile|LoadConfigReadsStaticFieldsFromTomlConfig|MemoryManagerConfigFromAppConfigDefaultsToStablePublicPath|MemoryManagerConfigFromAppConfigIgnoresDeprecatedExperimentalFields|MemoryQueryParamsIgnoreExperimentalFields|ConfigExampleOmitsExperimentalMemoryFields|NormalizeConfiguredToolLists|ToolSelectionPolicy_ApplyAddsAllowlistAndHonorsBlocklist|ToolSelector_|SessionTurnPreparer_SelectToolsForTurn_|SessionHistoryBuilder_BuildHistoryWithResolvedQuestionsReturnsAnsweredQuestions|HandleRSSInboxListGetAndPoll|BusRSSInboxActions|RSSInboxServiceBuildBriefingUsesAIHighlights)$'` 验证。
- `2026-03-10`：将 `core/bridge/app` 的 `ConfigStore` 收口为稳定运行时快照：`RuntimeConfig()` 不再在每次读取时重新回读环境变量覆盖内部状态，`loadMutationStateLocked()` / `persistLocked()` 也改为基于 store 内已有 snapshot 叠加文件变更，而不是把当前进程 env 当成隐式高优来源。补充 `config_store_persistence_test.go` 回归覆盖“env 漂移不影响已创建 store”和“Update materialize provider 时继续沿用初始 snapshot fallback”。尝试执行 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestConfigStore(ProviderCRUDPersistsToml|RuntimeConfigRemainsSnapshotAfterEnvChanges|UpdateKeepsSnapshotFallbackInsteadOfReloadingEnv)'`，但被当前工作树中既有 `rss_briefing.go` / `rss_inbox_service.go` 与现有 `Config` 结构不匹配的编译错误阻塞，未完成包级验证。
- `2026-03-10`：收紧 `core/bridge/app` 的 `AGENT_SEND` 语义，`prepareAgentTurnRequest(...)` 现要求 `message` 必须为非空字符串，不再允许仅凭 `session_id` 触发“空输入续跑”；`/api/agent`、`/api/agent/stream` 与 bus `AGENT_SEND` 回归测试同步改为校验该坏请求。与此同时，`ask_human` 的续跑链路从 `AGENT_SEND` 旁路拆出，`resumeAgentAction` / `resumeAgentStreamAction` 直接走内部 runner，不再复用公开 action 的空消息语义。已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(AgentEndpoint|HandleAgentStream|BusAgentSend|QuestionAnswerEndpoint)'` 验证。
- `2026-03-10`：按“仅替换前端样式、不新增业务”的范围重做 `apps/web` 聊天输入框：`ChatComposer` 改为你提供的白色圆角卡片式 composer，补齐自适应高度 textarea、底部胶囊模型选择器与圆形 send/stop 按钮，附件入口仅保留占位视觉不接上传逻辑；同步重写 `app/styles/composer.css` 以覆盖旧的亮色 composer 皮肤。已通过 `pnpm --dir apps/web exec tsc --noEmit` 验证。
- `2026-03-10`：拆掉 `core/bridge/app` 中 `bridgeService` 构造阶段的 RSS/task 持久化副作用：`newBridgeService(...)` 不再自动初始化 RSS inbox runtime 或写入 RSS 系统任务，新增显式 `StartBackgroundRuntimes()` 与 `BootstrapSystemTasks()` 两步启动；`runServer(...)` 改为在启动时显式调用，测试夹具只启动 runtime 不自动 bootstrap 系统任务。与此同时，任务列表默认只返回用户 `agent_message` 任务，新增 `/api/system/tasks` 单独查看系统任务，避免 RSS 系统任务污染 `/api/tasks` / `TASK_LIST` 的用户心智与测试基线。补充 `transport_tasks_test.go` 回归覆盖“显式 bootstrap 前不写系统任务”和“系统任务不进入默认任务列表”，并通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(HandleTasksCRUD|BusTaskCreateAndList|BootstrapSystemTasksStaysOutOfUserTaskLists|BridgeServiceRequiresExplicitSystemTaskBootstrap|HandleTaskUpdateDisableAndRunLogs|TaskSchedulerUsesProvidedSessionAndLogsRun|TaskSchedulerLeavesSessionBlankToOpenNewConversation|TaskSchedulerSkipsConcurrentRun|TaskSchedulerStopCancelsRunningTask|TaskSchedulerRunsMemoryHygieneSystemActionAndLogs|HandleRSSInboxListGetAndPoll|BusRSSInboxActions)'` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(Task|Tasks|RSS|AgentRuntimeFactoryRegistersTaskManageWhenTaskManagerPresent)'` 验证。
- `2026-03-10`：收紧 `send_file` 工具结果输出：对外 `SendFileResult` 使用 public artifact payload，不再携带 `stored_path`；`stored_path` 仅保留在落盘元数据中，避免 tool 原始输出/会话历史污染并提升跨环境可迁移性。已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./tools -run 'TestSendFile'` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestBuildSessionMessagePayloadProjectsSendFileAttachment'` 验证。
- `2026-03-10`：修复 `core/bridge/agent` 的非法 tool_call 历史污染问题：`runWithSink(...)` 在 `finish_reason=tool_calls` 分支不再先把原始 assistant `tool_calls` 直接写入 history，而是先做协议校验；全非法时改写为普通 assistant 错误说明，部分非法时只保留合法 `tool_calls`，并在发生重写时主动清空 provider `ConversationState`，避免 Codex `previous_response_id` 等续跑状态与本地重写历史失配。`core/bridge/llm` 同时新增统一 request 校验，OpenAI / Anthropic / Codex 在本地构造 request 前会拒绝缺 `tool_call.id`、空/非 object arguments、无配对 `tool_result` 或 dangling assistant tool_calls 的脏历史，防止继续向上游发送非法协议序列。补充 `loop_tool_test.go`、`loop_run_test.go` 与 `request_validation_test.go` 回归，并通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./agent -run 'TestRun(InvalidToolArgumentsWritesErrorEnvelope|EmptyToolArgumentsWritesErrorEnvelope|MissingToolCallIDWritesErrorEnvelope|MixedValidAndInvalidToolCallsOnlyReplaysValidCalls|WithPreloadedConversationStatePassesPreviousResponseID|InvalidToolCallClearsConversationStateForNextTurnReplay|ToolCallsThenStopWithSuccessEnvelope|ToolNotFoundWritesErrorEnvelope|ToolExecuteErrorWritesErrorEnvelope|RepeatedNonExecutableToolCallsReturnsErrorEarly|AskHumanReturnsAwaitingError|BrowserScreenshotKeepsToolRoleWithImageContent)'`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./llm -run 'Test(ToOpenAIRequest|OpenAIToCompletionResponse|ToAnthropicRequest|AnthropicToCompletionResponse|ToCodexRequest|Codex|CompleteCodex|CompleteStreamCodex)'` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(SessionHistoryBuilder|QuestionAnswer|Human|SessionAgentRunner)'` 验证。
- `2026-03-10`：继续修复第三方 Codex/Responses 网关在 Android `ask_human` 续跑中的兼容性问题，确认真正阻塞点不是首轮 request shape，而是 `core/bridge/llm/client.go` 的流式 `CompleteStream()` 之前缺少与非流式一致的 Codex stateless fallback：当带 `previous_response_id` 的 continuation 遇到网关 `400 upstream_error` 时，现会自动切换到无状态 replay；`core/bridge/llm/codex.go` 同步抽出流式请求构造以复用普通/无状态两条路径，`stream_test.go` 新增首跳 400、二跳 SSE 成功的回归。已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./llm -run 'Test(ToCodex|Codex|CompleteCodex|CompleteStreamCodex)'` 验证。
- `2026-03-10`：修复 `core/bridge/agent` 失败回合污染 history 的问题：`runWithSink(...)` 不再直接在主 `History` 上边跑边提交，而是每轮先克隆临时 history，用户消息、assistant 消息、tool result 与 provider `ConversationState` 都先写入该副本；仅在 `finish_reason=stop`、带文本的 `finish_reason=length` 或 `ErrAwaitingHuman` 这类允许中断时才 commit 回 Agent。失败路径现在会完整回滚本轮半成品，`GetNewMessages()` 也只再暴露已提交消息。补充 `loop_run_test.go` / `loop_tool_test.go` 回归覆盖失败不污染历史、conversation state 回滚与 awaiting-human 提交语义，并通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./agent` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(QuestionAnswerEndpoint|SessionHistoryBuilder)'` 验证。
- `2026-03-10`：继续收紧 `core/bridge` 的 Codex 兼容层，针对第三方 Responses 网关首轮 `400 upstream_error` 再补两层协议对齐：一是 `core/bridge/llm/codex.go` 现对 tool schema 做 sanitize，自动把 `integer` 归一为 `number`，补齐缺失的 object/array `type` 与空 `properties/items`；二是把 `browser_action` / `screen_action` 的开放式 `params` schema 改为显式字段集，避免向网关发送 `additionalProperties=true` 的任意键 object。此前同文件已补齐 `input[type=message]`、`tool_choice="auto"`、`parallel_tool_calls=false` 与 `strict=false`。同步扩展 `codex_test.go`、`browser_action_test.go` 与 `screen_action_test.go` 回归；已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./llm -run 'Test(ToCodex|Codex|CompleteCodex)'` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./tools -run 'Test(BrowserAction|ScreenAction)'` 验证。
- `2026-03-09`：为 `core/bridge` 新增首版 `codex` provider，专门适配 OpenAI Responses API 而不是继续复用 `chat/completions`：`core/bridge/llm` 新增 `codex.go`，支持把现有统一 `CompletionRequest/Response` 映射到 `model + input + tools` 的 Responses 协议，并解析 `message` / `function_call` 输出与 `response.output_text.delta`、`response.function_call_arguments.delta` 等流式事件；`core/bridge/app` 同步补齐 `codex` 的 provider 枚举、默认 `/responses` 路径推断、API key 校验与上下文预算；`core/shared/schema.json`、Web ConfigPanel/ModelSelector 与 CLI provider 标签已同步识别 `codex`。随后继续修复 Codex 多轮会话在第二轮 400 的问题：新增 `llm.ConversationState`，在 `agent.History` / `session.Session` 中持久化 `previous_response_id`，并让 Codex 在存在该状态时仅发送 `system + 上一条 assistant 之后的增量消息`，避免对 Responses API 重放整段历史。已通过 `python3 task.py gen-contracts`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'Test(InferProviderTypeDetectsCodexModel|ValidateRuntimeForExecutionRequiresAPIKeyForCodex|LoadConfigWithRuntime_LoadsToolSelectorSettings)'`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./agent ./session ./app -run 'Test(StoreSaveAndLoadSession|RunWithPreloadedConversationStatePassesPreviousResponseID|InferProviderTypeDetectsCodexModel|ValidateRuntimeForExecutionRequiresAPIKeyForCodex)'`、提权后 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./llm -run 'Test(Codex|CompleteStreamCodex)'`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./llm -run 'Test(ToCodexRequestUsesPreviousResponseIDAndOnlySendsIncrementalMessages|Codex)'` 与 `pnpm --dir apps/web exec tsc --noEmit` 验证。
- `2026-03-09`：继续修复第三方 Codex 网关（`https://lldai.online/openai`）在多轮/工具续跑时对 Responses API 的兼容问题：确认该网关既不接受 `previous_response_id`，也不接受手工回放的 `assistant` 历史；因此 `core/bridge/llm/codex.go` 为 stateless fallback 改为把已完成历史压缩成单条 synthetic `user` 上下文，仅保留当前轮的 `function_call` / `function_call_output`，并在 `core/bridge/llm/client.go` 对带 `previous_response_id` 的 `400 upstream_error` 自动重试这一 fallback。补充 `codex_test.go` 回归覆盖工具续跑 fallback、纯文本 follow-up fallback 与 synthetic user prompt 压缩；已通过 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./llm -run 'Test(ToCodex|Codex|CompleteCodexFallsBackToStatelessReplayAfterContinuation400|CompleteCodexFallsBackToStatelessReplayForPlainFollowUpTurn|ToCodexRequestWithOptionsStatelessCompressesHistoryIntoSyntheticUserPrompt)'`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./agent ./session ./app -run 'Test(StoreSaveAndLoadSession|RunWithPreloadedConversationStatePassesPreviousResponseID|InferProviderTypeDetectsCodexModel|ValidateRuntimeForExecutionRequiresAPIKeyForCodex)'`，并用真实 `curl` 验证同一 `session_id` 下“第一句触发工具 + 第二句再次触发工具”已恢复成功。
- `2026-03-09`：为 AI 增加首个桌面文本输入工具：`core/shared/schema.json` 新增 `TEXT_INPUT` action，并同步生成 Go/Rust/TypeScript/Kotlin envelope 常量；`drivers/native` 在 `host_actions` 中补齐聚焦输入框文本录入能力，Linux 通过 `xdotool type/key` 输入文字与换行，macOS/Windows 通过 `enigo` 键盘事件输入，支持 `submit=true` 追加回车提交；`core/bridge/tools` 新增 `text_input` tool，并接入 runtime registry，供 Agent 在已聚焦文本框后直接输入文字而不必退回 `script_exec`。已通过 `python3 task.py gen-contracts`、`cargo test --manifest-path drivers/native/Cargo.toml host_actions`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools -run 'TestTextInputTool|TestScreenActionParametersExposeExplicitOpenObjectSchema|TestScriptExecToolExecuteSuccess'` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run '^$'` 验证。
- `2026-03-09`：为桌面感知链路补齐首版本地 OCR/图标匹配能力：`core/shared/schema.json` 新增 `SCREEN_OCR` / `ICON_MATCH` action，并同步生成 Go/Rust/TypeScript/Kotlin envelope 常量；`drivers/native` 新增 `screen_vision` 模块，支持主屏或局部区域截图、调用本机 `tesseract` 做 TSV OCR、按绝对坐标返回文字框，以及基于模板匹配返回无字图标候选；`core/bridge/tools` 新增 `screen_action` 工具，提供 `ocr_scan` / `click_text` / `find_icon` / `click_icon` 四个动作，并接入 runtime registry、selector metadata 与默认 prompt 指引，优先引导 Agent 先点文字、再点图标。已通过 `python3 task.py gen-contracts`、`cargo test --manifest-path drivers/native/Cargo.toml`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools -run 'Test(ScreenAction|GetToolMetadata|FormatMetadataFor|BrowserActionParametersExposeExplicitOpenObjectSchema|BrowserActionToolMapsActionAndReturnsJSON)'`、`env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./context` 与 `env GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run '^$'` 验证。
- `2026-03-09`：为 `core/bridge` 增加工具名单硬约束配置：新增 `tool_allowlist` / `tool_blocklist`（以及 `GHOST_TOOL_ALLOWLIST` / `GHOST_TOOL_BLOCKLIST`）配置解析、归一化与校验，空名单时保持现有 selector 行为；一旦配置后，名单优先级高于 LLM selector，黑名单工具会在执行侧被强制移除，白名单工具会在 selector 子集结果上被强制补回，`ask_human` 继续保持 always-on。同步让 selector prompt 仅暴露名单裁剪后的可见工具，并补充配置加载、策略裁剪、selector 组合与 catalog metadata 回归；已通过 `go test -C core/bridge ./app ./tools -run 'Test(LoadConfigWithRuntime_(LoadsToolSelectorSettings|RejectsToolListOverlap|RejectsUnknownToolInList)|LoadConfigReadsStaticFieldsFromTomlConfig|NormalizeConfiguredToolLists_(RejectsOverlap|RejectsUnknownTool|IgnoresAskHumanBlocklist)|ToolSelectionPolicy_ApplyAddsAllowlistAndHonorsBlocklist|SessionTurnPreparer_SelectToolsForTurn_(AppliesBlocklistWhenSelectorDisabled|AppliesAllowlistToSubset|PassesPolicyScopedCatalogToSelector)|FormatMetadataFor(Catalog_FiltersToVisibleTools|Selector_ListsAllTools))'` 验证。
- `2026-03-09`：继续排查 `apps/android` “又闪退”反馈，补齐 `ChatViewModel` 启动/会话绑定/历史加载/附件打开链路的异常兜底与 `Log.e("GhostOS-ChatViewModel", ...)` 日志，避免 `observeBridgeConnection`、`observeSessionBinding`、`loadSessionHistory`、`refreshSessions` 等主协程上的未捕获异常直接带崩界面。重新执行 `./gradlew :app:compileDebugKotlin`、`./gradlew :app:assembleDebug`、`adb install -r` 与真机 `adb shell am start -W -n dev.ghostos.android/.MainActivity` 后验证，当前应用可稳定前台驻留、`crash` buffer 无新增致命异常；新日志同时确认设备侧对配置的 `http://100.104.14.2:8080` 为 `ECONNREFUSED`，即当前剩余问题是 Bridge 端口未监听/未开放，而非 Android 客户端闪退。
- `2026-03-09`：为 AI 回传本机文件补齐首版附件链路：`core/shared/schema.json` 新增 `EXPORT_FILE` action 与 `sessionFileContent`，`drivers/native` 新增 `EXPORT_FILE` 原子能力以在沙箱允许目录内复制文件快照并返回 `artifact_id / mime_type / bytes / sha256 / stored_path`；`core/bridge` 新增共享 `artifacts` store、`send_file` tool、`/api/sessions/{id}/artifacts/{artifact_id}` 下载接口，并在 `session_contract` 中把 `send_file` tool result 投影为会话 `content[type=file]`。Web 侧补齐附件 DTO 解析、消息附件卡片与下载代理路由；Android 侧补齐附件 DTO、消息卡片、带鉴权下载到缓存后通过 `FileProvider` 打开本地临时文件，同时顺手修复 `BridgeClient.stopRun()` 对缺失 `AgentStopParams` 的编译引用，当前已通过 `go test -C core/bridge ./app -run 'TestBuildSessionMessagePayloadProjectsSendFileAttachment|TestHandleSessionArtifactDownloadReturnsBinaryFile'`、`go test -C core/bridge ./tools -run 'TestSendFileToolExecute'`、`cargo test --manifest-path drivers/native/Cargo.toml file_actions`、`pnpm --dir apps/web exec tsc --noEmit`、`./gradlew :app:compileDebugKotlin` 与 `./gradlew :app:assembleDebug` 验证；真机已通过 `adb install -r` 安装最新 `app-debug.apk`、`adb shell am start -n dev.ghostos.android/.MainActivity` 拉起应用，当前 `dev.ghostos.android` 进程存活且 `crash` buffer 无新增致命异常。
- `2026-03-09`：开始收缩 `core/bridge/app` 的目录体积，先将任务域的模型与存储实现从 `core/bridge/app` 抽离到新子包 `core/bridge/tasks`：迁移 `ScheduledTask` / `TaskRunLog` / `TaskStore` 及任务文件持久化、run log 保留策略等底层实现，`app` 侧改为保留 bus/usecase/HTTP 编排与任务定义校验，并通过兼容桥接层继续暴露原有类型名，避免大面积调用点抖动。补充 `textutil.go` 承接通用截断函数，随后执行 `go test -C core/bridge ./app -run '^$'` 完成整包编译校验，并通过 `go test -C core/bridge ./app -run 'TestTask(Store|Scheduler)|TestTasks|TestTaskScheduler|TestTaskStore'` 验证任务相关回归。
- `2026-03-09`：修复 `apps/android` 启动即闪退的 Compose 运行时兼容问题：真机 `adb logcat -b crash` 定位到 `java.lang.NoSuchMethodError`，崩溃发生在 `ChatScreen` 的 `CircularProgressIndicator()`（`ProgressIndicatorKt` 调用 `animation-core` 方法签名不匹配）。先采取止血方案，将聊天页顶部和会话抽屉内的 Material progress indicator 替换为静态文本状态，避免继续命中该动画实现。随后重新执行 `./gradlew :app:assembleDebug`、`adb install -r` 并通过 `adb shell am start -n dev.ghostos.android/.MainActivity` 真机验证，当前 `MainActivity` 已前台驻留、`dev.ghostos.android` 进程存活，`crash` buffer 无新增致命异常。
- `2026-03-09`：修复 `apps/android` 的 `AGENT_STOP` 编译缺口并恢复可打包状态：在 Android 端补齐缺失的 `AgentStopParams` DTO，解决 `BridgeClient.stopRun()` 在 `:app:compileDebugKotlin` 阶段因未解析类型而失败的问题。随后重新执行 `./gradlew :app:assembleDebug` 已通过，并产出 `apps/android/app/build/outputs/apk/debug/app-debug.apk`，同时复制到 `/media/ruan/Apps/Android/artifacts/ghost-os-android-debug.apk` 供安装验证。
- `2026-03-09`：将 `core/bridge/tools` 的 `web_search` 切到 Tavily API：新增 `web_search_tavily_api_key` 配置项与 `GHOST_WEB_SEARCH_TAVILY_API_KEY` / `TAVILY_API_KEY` 环境变量读取，`agent_runtime_factory` 会把 key 注入 `tools.NewWebSearchTool(...)`；工具层现优先走 `POST https://api.tavily.com/search`，按 Tavily JSON 结果解析标题/链接/摘要，未配置 key 时才回退旧的 DuckDuckGo HTML + Bing RSS 链路。补充 `TestWebSearchToolUsesTavilyWhenConfigured`、配置文件读取与环境变量读取回归，并通过 `go test -C core/bridge ./tools/...`、`go test -C core/bridge ./app -run 'TestLoadConfigWithRuntime_LoadsToolSelectorSettings|TestLoadConfigWithRuntime_LoadsWebSearchTavilyAPIKeyFromEnv|TestLoadConfigReadsStaticFieldsFromTomlConfig|TestRuntimeConfigFromEnvPrefersMultiProviderConfigFile'` 验证；同时已将本机 `~/.ghost-os/config.toml` 写入 Tavily key，并实测 Tavily 官方接口可返回搜索结果。
- `2026-03-09`：修复 `apps/android` 会话事件订阅导致的发送后闪退：`BridgeClient.observeSessionEvents()` 不再额外包一层未 `awaitClose` 的 `callbackFlow`，直接复用底层 `observeEventStream(...)`，避免首次拿到 `session_id` 后订阅 `/api/sessions/{id}/events` 时抛出 `IllegalStateException`；同时补上 `BridgeClientTest` 覆盖 SSE 会话事件消费，并顺手对 `session_id` 做 URL 编码。尝试执行 `./gradlew testDebugUnitTest` 时被本机 Android 环境配置阻塞（`sdk.dir=/media/ruan/Apps/Android/sdk`，Gradle 在应用 `com.android.application` 时即失败），因此本次仅完成代码级修复与回归补充，待本机 SDK 路径恢复后再做任务级验证。
- `2026-03-09`：修复 `core/bridge/tools` 的 `web_search` 单点失效问题：原实现只请求 `https://html.duckduckgo.com/html/`，在当前环境中实测持续连接超时，导致工具连续失败。现改为多 provider 降级链路，默认先试 DuckDuckGo HTML，再自动回退到 `https://www.bing.com/search?format=rss`；同时按 provider 拆分请求超时预算，避免首个坏端点耗尽整次搜索时间，并在最终报错中串出各 provider 失败原因。新增 `rss_parser.go` 与 `TestWebSearchToolFallsBackToBingRSS` 回归，`go test -C core/bridge ./tools/...` 已通过。
- `2026-03-09`：继续收紧 `core/bridge/memory` 的 Memory Palace 策展质量：`palace_collect.go` 新增针对工具能力/配置问答、RSS/`ask_human` 命名工具壳、assistant 复述与 task-local residue 的过滤，并清洗 decision memo 中的执行结果/确认话术；`palace_prompt.go` 明确要求对 named-tool usage、capability/config chatter 默认走 candidate/reject；`palace_curator.go` 补上 post-LLM 准入门槛，`stable` 现要求跨会话复现或更耐久的多层辅证，单会话 `decision + cold` echo 会降为 `candidate`，工具/报错噪声会降为 `reject`，且阈值只基于 LLM 最终采用的 evidence 计算。补充 `palace_curator_test.go` 覆盖工具壳降级与单会话 decision echo 降级，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'Test(CollectPalaceEvidenceIncludesSourceMetadata|RunPalaceCurationWritesReportAndLatest|FinalizePalaceTopicDowngradesToolChatterStable|FinalizePalaceTopicDowngradesSingleSessionDecisionEchoToCandidate)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestBusMemoryPalaceCurateRun'` 验证。使用真实 `~/.ghost-os` 数据经临时 bridge dry-run 生成 `~/.ghost-os/memory/palace/reports/2026-03/report-mpr-20260309-055200.{md,json}`，统计从上一版的 `stable=9 candidate=0 reject=0` 收敛到 `stable=3 candidate=2 reject=1`，`latest.md` 已同步更新。
- `2026-03-09`：新增只读记忆策展 action `MEMORY_PALACE_CURATE_RUN`：`core/bridge/memory` 新增 `palace_*` pipeline（collect / cluster / discovery / judge / render / store），只读扫描 `decision / warm / cold / markdown / graph / truth`，通过现有 worker client 做两段式 LLM 策展，并将结果落盘到 `~/.ghost-os/memory/palace/reports/YYYY-MM/report-<id>.md|json`，同时更新 `~/.ghost-os/memory/palace/latest.md`，不再写回 decision/hygiene/truth。`core/bridge/app` 已接入新 bus action、参数/响应类型与 worker 适配，`core/shared/schema.json` 和生成的 envelope 常量已同步；补充 `palace_curator_test.go` 与 `memory_palace_actions_test.go` 覆盖 evidence metadata、report 落盘与 bus 触发，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'Test(CollectPalaceEvidenceIncludesSourceMetadata|RunPalaceCurationWritesReportAndLatest)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestBusMemoryPalaceCurateRun'` 验证。另用真实 `~/.ghost-os` 配置在 `127.0.0.1:18080` 手动触发 dry run，成功生成 `report-mpr-20260309-053148.md|json`。
- `2026-03-09`：补齐 `core/bridge/app` RSS report 的“事件出处”落盘链路：`BuildAndStoreReport()` 现会在保存前把来源直接内联进 `## 发生了什么`，按 briefing highlight / topic group 为每个事件块补 `出处：...`，映射到底层 source items，并带出 source title、link、发布时间，避免模型漏写来源时报告失去可追溯性；同时增强 report prompt 与 dossier，让调查 agent 明确知道每个事件必须在“发生了什么”里紧跟出处，`renderRSSReportDossier()` 也开始把 source title 写入原始资料。扩展 `rss_report_test.go` 覆盖正文内联补出处、fallback 仍内联出处、dossier 含来源标题与 prompt 强制内联出处约束，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestRSSInboxServiceBuildAndStoreBriefingWritesReportMarkdown|TestAgentRSSReportBuilderUsesScopedToolsAndDossier|TestRSSInboxServiceBuildAndStoreBriefingFallsBackWhenAgentReportFails|TestRenderAgentRSSReportPromptRequiresChinese'` 验证。
- `2026-03-09`：为 `core/bridge` 新增 decision relation dry-run 管道与定时任务入口：`core/bridge/memory` 增加 `RunDecisionRelations(...)`、LLM relation prompt/worker 接口、增量 memo 扫描、checkpoint（`~/.ghost-os/memory/palace/relation-runs/checkpoint.json`）、accepted/rejected 校验去重与 `~/.ghost-os/memory/palace/relation-runs/YYYY-MM/run-*.{md,json}` 报告落盘；`core/bridge/app` / `core/shared/schema.json` 新增 `MEMORY_DECISION_RELATION_RUN` bus/system_action，可通过 task scheduler 周期执行并返回 `scanned_memos/eligible_memos/extracted_candidates/accepted_relations/rejected_candidates` 统计。当前 `inject_graph=true` 仍显式拒绝，先只支持 dry-run/report/checkpoint。补充 `decision_relation_runner_test.go` 与 `memory_decision_relation_actions_test.go`，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestRunDecisionRelations'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(BusMemoryDecisionRelationRun|TaskSchedulerRunsMemoryDecisionRelationSystemAction)'` 验证。
- `2026-03-09`：收紧 `core/bridge/memory` legacy hygiene 止血路径：`collectProjectionHygieneCandidates()` 已停止扫描 `decision.memo`，`inferHygieneCardKind()` 暂停新的 `convention` 合成，并在 `hygieneLooksTransient(...)` 增加对普通问答壳、寒暄和一次性工具说明的拒绝规则；同时修复 `HygieneService.SyncWarmProjection()`，让 `cards/` 删除后会同步清理 warm 中残留的 `hygiene_rule` 投影。补充 `hygiene_runner_test.go` 与 `hygiene_service_test.go` 回归，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'Test(Hygiene|RunHygiene|BuildContextWindowPrependsRecipeAdvisory|MemoryManagerBuildContextWindowAutoRecall)'` 验证；实盘已清理 `~/.ghost-os/memory/hygiene/cards` 中 10 张 `decision.memo` 垃圾卡并重建 `index.json`，`projection` dry-run 结果为 `scanned=0 selected=0`，`warm.json` 的 `hygiene_rule` 投影已从 12 条收敛到 2 条，`sql/ask_human/linux.do/latest.rss` 关键词不再命中已删 hygiene 卡，并确认当前 `~/.ghost-os/tasks/tasks` 下不存在 `MEMORY_HYGIENE_RUN` 定时任务。
- `2026-03-09`：修复 `core/bridge/memory` decision admission 对“有实质总结但结尾带追问”的 assistant 回复误判为 `low_signal_turn` 的问题；`decisionTurnHasActionableAssistantContent(...)` 与 `decisionTurnHasMeaningfulSummary(...)` 改为按内容片段而不是整条回复做判断，避免正常偏好总结/执行约束因为尾部 `？` 被整轮拒写。新增 `admission_policy_test.go` 覆盖“actionable + question”可落盘与“纯追问”仍拒写，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestAdmissionPolicyShouldCaptureDecision'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestSessionTurnStateComplete_(CapturesDecisionMemoOnSuccess|SkipsLowSignalDecisionTurn)'` 验证；同时用真实配置克隆实例在 `127.0.0.1:8081` 复现确认，`~/.ghost-os/memory/decision/memos.json` 已能写入该类偏好记忆。
- `2026-03-09`：完成 memory 记忆收口的两条主线：`core/bridge/memory/internal/graph` 现已关闭 `archive/raw chat -> graph` 直抽入口，`GraphService.IngestArchiveMessages(...)` 与 graph index projector 均改为停用，`graph_backfill.go` 也不再扫描 archive；同时在 `graph_admission.go` 收紧句法闸门，新增祈使句、未消解代词主语与“非陈述句 claim”拒抽，graph 只继续吃 markdown node 和 truth-backed projection。另一侧在 `core/bridge/memory` 新增环境快照 scoped fact：运行前会把当前 `session/toolset signature/provider/model/graph namespace/workspace/time` 写成 `environment.snapshot` truth object，并生成 `capability.tool.available|unavailable` 与 `config.*.active` claim；truth query/hybrid recall 现按环境指纹优先匹配并在同一 `fact_key` 下只选最新最佳 scope，debug 会带 `environment_score` / `scope_match_fields` / `scope_filtered_out`。补充 `truth_query_primary_test.go`、`memory_hybrid_query_test.go`、`internal/graph/graph_query_test.go`、`index_runtime_test.go` 回归，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory/internal/graph -run 'TestGraph(RetrieveSeedsFromClaimLineage|IngestArchiveMessagesIsDisabled|MarkdownExtractionSkipsImperativeAndPronounSegments)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestTruthReaderQueryPrimary(ActiveOnlyDefault|IncludeHistoricalAndConflicts|MatchesObjectAndValue|PrefersScopedEnvironmentFact)|TestHybridQueryScopesEnvironmentFactsByToolset|TestQueryResultWithScopeIncludesGraphRecallHits|TestMemoryManagerRunIndexOnceSkipsGraphProjectionFromLedger'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run '^$'` 验证。
- `2026-03-09`：将 `core/bridge` 的 hygiene 从“垃圾评分/隔离”改造成 synthesis 规则卡片流：`core/bridge/memory` 新增 `HygieneCard` 模型、`cards/*.md + cards/index.json` 人类可读存储、working-memory/markdown/decision 聚合提炼、重复运行收敛与 warm recall 投影；`BuildContextWindow` 与 hybrid query 现优先注入/返回 `memory_kind=hygiene_rule` 卡片，legacy `ScoreGarbage/GetHygiene/ListQuarantined` 仅保留兼容壳与 metadata 装饰，不再承担读侧抑制主语义。`core/bridge/app` / `core/shared/schema.json` 的 `MEMORY_HYGIENE_RUN` 参数已切到 `scope + limit + dry_run + session/window + force_refresh + min_confidence`，响应改为 `selected/created/updated/skipped_duplicates` synthesis 统计，同时继续兼容旧 `warm/report/max_votes_per_run/reason_codes` 调用壳。补充 `hygiene_runner_test.go`、`hygiene_service_test.go`、`hygiene_read_filter_test.go`、`session_turn_preparer_hygiene_test.go` 与 `transport_tasks_test.go` 回归，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'Test(Hygiene|RunHygiene|BuildContextWindowPrependsRecipeAdvisory|MemoryManagerBuildContextWindowAutoRecall)'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(SessionTurnPreparerInjectAutoRecallPrioritizesHygieneRules|HandleTaskCreateSystemActionWithoutMessage|BusMemoryHygieneRun|TaskSchedulerRunsMemoryHygieneSystemActionAndLogs)'` 验证。期间顺手补了当前工作树里已存在但会阻断编译的 `memory_manager_assemble.go` / `graph_service.go` / `internal/graph/graph_backfill.go` / `truth_query.go` 兼容缺口，使相关包能重新进入定向回归。 
- `2026-03-09`：完成 memory admission gate 首轮收口：`core/bridge/memory/admission_policy.go` 新增统一准入规则，`StoreWorkingMemory(...)` / `DecisionService.CaptureTurn(...)` / `GraphService.IngestArchiveMessages(...)` 现统一走“默认不记，满足条件才写”；`core/bridge/memory/internal/graph/graph_admission.go` 与 `graph_extractor.go` 进一步把问句、寒暄、低信号碎片拦在 segment/message 级，避免 graph sidecar 对原始句子无差别长 node。同步补充 warm 噪声拒写、session turn 低信号 decision skip、index replay 对 question-only graph/low-signal decision 的回归，以及 internal graph 的问句拒抽测试。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory/internal/graph -run 'TestGraph(RetrieveSeedsFromClaimLineage|RetrieveMarksLegacyFallback|IngestArchiveMessagesSkipsQuestionSegments)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManager(StoreWorkingMemory|RunIndexOnce)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManager(StoreWorkingMemoryRejectsConversationNoise|RunIndexOnceSkipsLowSignalDecisionShadowStore|RunIndexOnceSkipsQuestionOnlyGraphProjection)'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestSessionTurnStateComplete_(CapturesDecisionMemoOnSuccess|CapturesDecisionMemoOnAwaitingHuman|DoesNotCaptureWhenPersistFails|CaptureErrorDoesNotAffectReturn|DoesNotCaptureWhenToggleDisabled|SkipsLowSignalDecisionTurn)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(BusMemoryQueryReturnsDecisionHits|SessionTurnStateComplete_CapturesDecisionMemoOnSuccess|SessionTurnStateComplete_CapturesDecisionMemoOnAwaitingHuman)'` 验证。
- `2026-03-09`：完成 memory/history 分层的优先级 1 收口：`core/bridge/app/session_turn_committer.go` 已停止在每轮提交后把原始会话消息镜像写入 warm，当前只保留 `session store` 历史持久化与 `ledger` append；`core/bridge/memory` 新增 `StoreWorkingMemory([]MemoryEntry)` 作为 warm 的显式写入口，并将 `PromoteToWarm(...)` 收紧为兼容 no-op，避免再把原始 history 投影为 memory。相应测试已改为显式 seed working memory，`session_turn_committer_test.go` 也补上“不会生成 warm snapshot”的断言。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManager(ArchiveToColdUsesLoadOnlySessionPort|ArchiveToColdReturnsLoadError|PromoteToWarm|StoreWorkingMemory|QueryCascadesHotWarmCold|BuildContextWindowAutoRecall|BuildContextWindowSkipsVisibleMessages|BuildContextWindowIsolatesSessionRecall)'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'Test(SessionTurnCommitterPersistSessionMessagesDualWritesLedger|SessionTurnPreparerInjectAutoRecallSkipsHygieneFlaggedEntries|BusMemoryHygieneRun)'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run '^$'`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run '^$'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 8/9（legacy backfill isolation + duplicate snapshot cleanup）：新增 `core/bridge/memory/internal/migrate` 承接 legacy cold snapshot 文件 IO 与 ledger backfill 编排，根包 `cold_legacy.go` / `ledger_backfill.go` 现只保留 `LegacyColdStore` 与 `LedgerStore.BackfillLegacy(...)` 的 façade 级类型适配；`core/bridge/app/cmd_memory_ledger_backfill.go` 保持公开 CLI 入口不变，但不再直接依赖根包里的迁移实现细节。同步删除根目录这条链上的 flat helper/implementation snapshot，`doc.go` 现明确 migration-only 逻辑应留在 `internal/migrate`。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestColdMemoryLedgerBackfillCompat|TestLedgerStoreAppendReplaySessionStableOrder|TestColdMemoryLedgerPrimaryReadReturnsLedgerEntries'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestCommandDispatcherMemoryLedgerBackfill|TestParseLedgerBackfillRangeSupportsCalendarBounds'` 验证。
- `2026-03-09`：将 RSS report 从单次 worker Markdown 生成升级为受限调查 agent：`core/bridge/app` 现新增 `agentRSSReportBuilder`，会先为每次 report 落一个 dossier 文件，再用 `agent.NewAgent(...) + tools.NewScopedCatalog(...)` 运行只读调查工具集（`read_file`、`read_and_summarize`、`web_search`、`rss_fetch`、`browser_action`）完成补充调查与最终成稿；默认 report builder 已从简单 `Complete()` 切到该调查 agent。report 仍保持 `briefing` 成功优先，agent 失败时自动回退到本地 Markdown fallback，不回滚 briefing。补充 `TestAgentRSSReportBuilderUsesScopedToolsAndDossier` 与 fallback 回归，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestRSSReportStore(SaveAndLatest|LatestNotFound)|TestRSSInboxServiceBuildAndStoreBriefingWritesReportMarkdown|TestAgentRSSReportBuilderUsesScopedToolsAndDossier|TestRSSInboxServiceBuildAndStoreBriefingFallsBackWhenAgentReportFails|TestRSSInboxServiceBuildBriefingUsesAIHighlights|TestHandleRSSInboxListGetAndPoll|TestBusRSSInboxActions|TestExecuteRSSInboxPollUsecaseUsesTaskID'` 验证。
- `2026-03-09`：将 RSS report 的成稿语言收紧为简体中文：`core/bridge/app/rss_report.go` 的调查 agent system/user prompt 现明确要求“整篇报告必须使用简体中文”，同时 fallback Markdown 的固定小节标题与说明文案也改为中文，避免 agent 失败时回退成英文报告；补充 `TestRenderAgentRSSReportPromptRequiresChinese` 并同步更新 report/fallback 回归断言。
- `2026-03-09`：将 RSS report 标题固定为携带精确生成时间：`rssReportTitleOrDefault(...)` 现统一输出 `... Report - YYYY-MM-DD HH:MM UTC`，`prepareRSSReportDraft(...)` 用 report `GeneratedAt` 生成标题；调查 agent prompt 也会显式要求最终 Markdown 的首个 H1 必须使用该精确标题，fallback Markdown 同步复用同一标题，避免 agent 成稿与回退稿的标题格式不一致。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 7（index façade thinning）：`index_runtime.go` 现只保留 `IndexRuntime` façade 与构造装配，graph/decision projector builder、turn-envelope 到 decision capture 的适配、shadow path 计算已拆到 `index_runtime_projectors.go`，ledger bucket source 与 segment 读取留在 `index_runtime_ledger_source.go`；`internal/indexer` 继续保持 projector/runtime/checkpoint/view-manifest 的内部实现，不做结构性改写。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManagerRunIndexOnceProjectsGraphFromLedgerAndWritesCheckpoint|TestMemoryManagerRunIndexOnceWritesDecisionShadowStore'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 6（query dependency tidy-up）：`QueryService` 现改为依赖 `warm/cold/graph/decision/hygiene/planner/bucket/vector` 的窄接口，而不是直接耦合具体实现；query 中散落的 bucket 规划/过滤辅助已回收到 `bucket_query_adapter.go`，markdown bucket/filter/mapping/anchor recall 辅助已回收到 `memory_markdown_query.go` 与 `ColdMemory.QueryMarkdown(...)` façade，`memory_query.go` / `memory_query_hybrid.go` 只保留编排逻辑，不再直接访问 `cold.markdown` 这类域内实现细节。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManagerQueryIncludesMarkdownNodes|TestHybridQueryBucketShadowProducesPlanAndReport|TestQueryResultWithScopeIncludesGraphRecallHits|TestPlannerHydrationScopeNarrowsEnabledLayers|TestBuildContextWindowPrependsRecipeAdvisory'` 验证。
- `2026-03-09`：补齐 RSS report 后端最小 MVP：`core/bridge/app` 新增 `RSSReportStore`（默认 `~/.ghost-os/rss/reports/index.json`）与 `llmRSSReportBuilder`，`RSS_BRIEFING_BUILD` / `BuildAndStoreBriefing()` 在 briefing 成功后会自动生成并落盘 Markdown 报告到 `~/.ghost-os/rss/reports/YYYY/MM/rssr_*.md`，并在返回结果里附带 `report.markdown_path` / `report.briefing_id` 等元数据；report 生成失败不会回滚 briefing，只会返回 `report_error`。新增 `rss_report_store_test.go`、`rss_report_test.go`，并通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestRSSReportStore(SaveAndLatest|LatestNotFound)|TestRSSInboxServiceBuildAndStoreBriefingWritesReportMarkdown|TestRSSInboxServiceBuildBriefingUsesAIHighlights|TestHandleRSSInboxListGetAndPoll|TestBusRSSInboxActions|TestExecuteRSSInboxPollUsecaseUsesTaskID'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 5（graph helper sink）：删除根包残留的 `graph_helpers.go` 本地 namespace normalize 实现，新增 `internal/graph/export_api.go` 作为 graph normalize 导出边界，并由根包 `graph_internal_adapter.go` 负责 façade/internal 转发；`graph_service.go` 继续保留 service-level adapter，`graph_models.go` 继续保留 public DTO，root 包不再承载 graph-specific normalize 实现。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestQueryResultWithScopeIncludesGraphRecallHits|TestGraphRetrieveSeedsFromClaimLineage|TestGraphRetrieveMarksLegacyFallback|TestMemoryManagerRunIndexOnceProjectsGraphFromLedgerAndWritesCheckpoint'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 4（decision helper sink）：根包 `decision_format.go`、`decision_helpers.go`、`decision_normalize.go`、`decision_recipe_query.go` 现改为通过 `decision_internal_adapter.go` 做 root/internal 类型转换，再调用 `internal/decision/export_api.go` 暴露的 helper/format/normalize/query 边界；decision 的具体 helper/format/normalize 实现继续收敛到 `internal/decision`，根包仅保留 façade 级 API 适配与 DTO clone 逻辑。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestMemoryManagerQueryResultIncludesDecisionHits|TestBuildContextWindowInjectsConciseDecisionRecall|TestBuildContextWindowDecisionRecallSkipsDuplicateSummary|TestDecisionRecallPrefersClaimLineageCandidates|TestDecisionRecallUsesRunClaimIndexForRecipeCandidates|TestRecipeSelectionPrefersEnvironmentMatchedRecipe|TestRecipeExecutionTrackingWritesRunAndFeedback|TestBuildContextWindowPrependsRecipeAdvisory|TestDecisionLineageExplainAPIs'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestTruthShadowDualWriteMarkdownDecision|TestSaveMarkdownNodeProjectsClaimLineage|TestMemoryManagerStopDreamingStopsDecisionDistiller|TestMemoryManagerDoesNotCreateDecisionDistillerWhenRecipeDisabled'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 3（markdown store sink）：`memory_markdown.go` 现只保留 public `MarkdownNode`、`MarkdownStore` façade 和 root/internal adapter；`MarkdownStore` 的文件 IO、manifest 索引、bucket 过滤、node normalizer 已下沉到 `internal/markdown/store.go`，内部使用独立 node/anchor/bucket 契约避免 root 包继续承载 cold projection 存储实现。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestColdMemorySaveAndLoadMarkdownNode|TestColdMemoryLoadLegacyMarkdownNodeWithoutStructuredFields|TestMemoryManagerQueryIncludesMarkdownNodes|TestHybridQueryBucketShadowProducesPlanAndReport|TestTruthShadowDualWriteMarkdownDecision|TestSaveMarkdownNodeProjectsClaimLineage'` 验证。
- `2026-03-09`：完成 `core/bridge/memory` façade/internal 收口的阶段 2（bucket type/impl split）：`bucket_manifest.go` 现只保留 `BucketKey / BucketPlan / BucketCandidate / BucketShadowReport` 等 root DTO 与极薄规范化适配，`normalizeBucket*`、`bucketManifestFromLedger` 的具体实现下沉到 `internal/bucket`；同时新增 root/internal bucket adapter，把 manifest/session/view 的归一化、ledger->bucket projector 与 planner 结果转换集中到边界层，避免 root 包继续承载 bucket 规则实现。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestLedgerBucketManifestCapturesSessionStatsAndTopTerms|TestBucketPlannerPrefersSessionAndRespectsBudget|TestHybridQueryBucketShadowProducesPlanAndReport'` 验证。
- `2026-03-09`：推进 `core/bridge/memory` façade/internal 收口的阶段 1：新增 `internal/anchor`、`internal/bucket`、`internal/markdown` 目录；将 `anchor_extractor.go` 的规则抽取逻辑与 `bucket_plan.go` 的 bucket 评分/预算选择逻辑下沉到 `internal/*`，根包分别保留 `MemoryAnchor` / `BucketPlan` 相关薄包装与类型转换；`anchor_claim_mapper.go` 也将 predicate/type/key 判定这类纯 helper 下沉到 `internal/anchor`，根包继续保留 truth claim 组装。legacy cold/ledger backfill 兼容链本阶段未动。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestExtractRuleAnchorsDetectsPreferenceAndAvoidance|TestBucketPlannerPrefersSessionAndRespectsBudget|TestHybridQueryBucketShadowProducesPlanAndReport'` 验证。
- `2026-03-09`：冻结 `core/bridge/memory` façade/internal 迁移边界的阶段 0：`doc.go` 现明确钉住永远留在根包 façade 的文件清单（`memory_manager.go`、`memory_manager_assemble.go`、`memory_query.go`、`memory_types.go`、`memory_config.go`、`decision_service.go`、`graph_service.go`、`index_runtime.go`），并补充“优先下沉 root-level 纯实现、暂不动 legacy cold/ledger backfill 兼容链”的目录契约，作为后续按能力域小批次迁移的护栏。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory -run 'TestNewMemoryManagerLogsStableAndExperimentalBoundaries|TestNewMemoryManagerStableConfigDoesNotAssembleExperimentalChain|TestNormalizeMemoryConfigDoesNotAutoEnableExperimentalIndexFromLedger'` 验证。
- `2026-03-09`：将 RSS AI briefing 扩展为可落盘、可定时、可直接阅读的主链：`core/bridge/app` 新增 `RSSBriefingStore`（默认 `~/.ghost-os/rss/briefings.json`）保存最近 briefing 快照，`GET /api/rss/briefing` 改为读取最新持久化 briefing，`POST /api/rss/briefing` 与 `RSS_BRIEFING_BUILD` 会基于聚合组调用 worker model 生成并保存结果；新增 `RSS_BRIEFING_GET` bus action，并让 `RSS_BRIEFING_BUILD` 可作为 `system_action` 定时任务运行，Bridge 启动时会自动确保 `system-rss-briefing-build` 注册。新增 `rss_briefings_path` / `rss_briefing_enabled` / `rss_briefing_interval` 配置项，且运行时更新配置后会重载 RSS runtime 并重建 RSS 相关 system task。`apps/web` 同步新增 `/api/rss/briefing` 代理、`useRSSBriefing()` hook、`RSSBriefingPanel` 右侧快报面板与手动生成/刷新按钮，首页现在可直接阅读最新 briefing。新增 `rss_briefing_store_test.go`、扩展 `rss_briefing_test.go` / `transport_rss_inbox_test.go`，并已通过 `go test -C core/bridge ./app -run 'TestRSSBriefingStore(SaveAndLatest|LatestNotFound)|TestRSSInboxService(BuildBriefingUsesAIHighlights|AggregateGroupsByTopicWindowAndImportance)|TestHandleRSSInboxListGetAndPoll|TestBusRSSInboxActions|TestExecuteRSSInboxPollUsecaseUsesTaskID'`、`pnpm --dir apps/web exec tsc --noEmit --pretty false` 与 `pnpm --dir apps/web build` 验证。
- `2026-03-09`：为 `core/bridge/app` 增加 AI 版 RSS briefing 首版：新增 `RSS_BRIEFING_BUILD` bus action 与 `GET /api/rss/briefing` 只读接口，复用 RSS inbox 聚合结果作为输入，调用 worker model 生成结构化快报 JSON（`title / summary / highlights[]`）；每条 highlight 保留 `group_id / topic_label / importance / source_item_count / source_feed_count / tags` 以追溯到底层聚合组，并在模型返回非法 `group_id` 或空 highlight 时回落到聚合结果生成稳定输出，避免直接返回空快报。新增 `rss_briefing.go`、`rss_briefing_test.go` 并扩展 `transport_rss_inbox_test.go`，已通过 `go test -C core/bridge ./app -run 'TestRSSInboxService(BuildBriefingUsesAIHighlights|AggregateGroupsByTopicWindowAndImportance)|TestHandleRSSInboxListGetAndPoll|TestBusRSSInboxActions|TestExecuteRSSInboxPollUsecaseUsesTaskID'` 验证。
- `2026-03-09`：推进 `core/bridge/memory` experimental runtime cutover 的第三阶段收口：`normalizeMemoryConfig()` 不再因 `ledger dual-write + stable projector` 自动启用 `Index`，`NewMemoryManager()` 也改为仅在显式开启时装配 `truth / vector / truthReader / index / planner`，稳定配置默认不再挂载 experimental 链。同步删除已无实际分支意义的 `Index.LegacyColdAsync` 与 `Truth.LegacyObjectProjectionEnabled` 残留字段，并补充 `memory_manager_assemble_boundary_test.go` 验证 stable 配置不装 experimental 组件、index 维持 opt-in。待验证项仍以 `core/bridge/memory` 包测试为准。
- `2026-03-09`：为 `core/bridge/app` 增加 RSS Inbox 聚合层首版：新增 `RSS_INBOX_GROUPS` bus action 与 `GET /api/rss/inbox/groups` 只读接口，基于现有 inbox 快照按时间窗、主题 tag/title token 重叠与重要度做二次分组，返回 `topic_label / headline / summary / importance / window_start / window_end / item_count / feed_ids / items` 等聚合视图字段，作为后续“新闻快报”成稿链路的基础。保留原有 `/api/rss/inbox` 平铺列表与 `RSS_INBOX_LIST` 语义不变。新增 `rss_inbox_aggregate_test.go` 并扩展 `transport_rss_inbox_test.go`，已通过 `go test -C core/bridge ./app -run 'TestRSSInboxServiceAggregateGroupsByTopicWindowAndImportance|TestHandleRSSInboxListGetAndPoll|TestBusRSSInboxActions|TestExecuteRSSInboxPollUsecaseUsesTaskID'` 验证。
- `2026-03-09`：推进 `core/bridge/memory` legacy config cutover 的第二阶段：`MemoryConfig` 已移除扁平 legacy alias，只保留 `Warm / Cold / Ledger / Graph / Decision / Hygiene / Truth / Vector / Recall / Index / Runtime` 结构化配置块；`normalizeMemoryConfig()` 不再调用 `applyLegacyMemoryConfig()`，`core/bridge/memory/memory_config_legacy.go` 已删除。`core/bridge/app` 同步移除 `memory_runtime_experimental.go` 与 `memory_ledger_dual_write` / `memory_ledger_shadow_compare` 的 file/env 桥接，不再把 deprecated shadow toggle 映射回 runtime；`memory_runtime_boundary_test.go` 改为验证旧字段会被忽略。相关 `memory` / `app` 调用方与测试构造已统一改成结构化 `memory.MemoryConfig`。已通过 `go test ./memory`（工作目录 `core/bridge`）验证；`go test ./memory ./app` 过程中 `./app` 仍被现有编译错误 `core/bridge/app/transport_server.go:116` 缺失 `handleRSSInboxGroups` 阻塞，与本次 memory/config 改动无直接耦合。
- `2026-03-09`：完成 `core/bridge/memory` legacy cold cutover 的第一阶段：`ColdMemory` 运行时已改为 ledger-only，`Archive` 不再写 legacy snapshot，`Retrieve` / `ListSessions` / `ListArchives` 与 bucket-plan 兼容视图全部直接收敛到 ledger replay；`LegacyColdStore` 已移除 truth/archive 双写挂钩，`MemoryManager` / `ColdMemory` 不再暴露常驻 `BackfillLedger` API。新增离线 CLI 子命令 `go run ./core/bridge memory ledger-backfill`，直接用 `LegacyColdStore + LedgerStore` 做一次性回填，支持 `--cold-dir` / `--ledger-dir` / `--namespace` / `--workspace-id` / `--from` / `--to` / `--dry-run`。同步补充 `cmd_memory_ledger_backfill_test.go`、更新 cold/ledger 回归与 truth shadow 测试，并已通过 `go test -C core/bridge ./memory ./app -run 'Test(ColdMemoryArchiveAndRetrieve|ColdMemoryArchiveUsesLedgerOnly|ColdMemoryListSessionsByTimeRange|ColdMemoryLedgerBackfillCompat|ColdMemoryLedgerPrimaryReadReturnsLedgerEntries|TruthShadowDualWriteMarkdownDecision|TruthArchiveDualWriteDoesNotChangeRetrieve|CommandDispatcherMemoryLedgerBackfill|ParseLedgerBackfillRangeSupportsCalendarBounds)$'`、`go test -C core/bridge ./memory` 与 `go test -C core/bridge ./app` 验证。
- `2026-03-08`：为 `apps/android` 补齐实时 AI 状态流体验：聊天发送链路改为消费 Bridge SSE，新增 `/api/questions/answer/stream` 以支持 `ask_human` 回答后的流式续跑；`core/bridge/app` 的 session push 现会额外广播 `run_started` / `completion_delta` / `tool_call_started` / `tool_call_finished` / `error` / `done` 等细粒度运行事件，保留原有 `assistant_message` / `awaiting_human` 兼容。Android 侧新增 `AgentStreamEvent` 模型与 `BridgeClient.streamMessage()` / `streamAnswer()` / `stopRun()`，`ChatViewModel` 改为统一事件状态机，可实时显示“正在思考”、正文增量、工具开始/完成、等待人工与错误日志，并支持“停止当前运行”按钮调用 `AGENT_STOP` 立即结束本机 active run；`ChatScreen` 同步升级为多类型消息气泡与 stop 按钮。新增 `transport_session_events_test.go` / `transport_human_answer_test.go` / `ChatViewModelTest.kt` 回归，已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'SessionEvents|QuestionAnswer|AgentStream'`、`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 验证。
- `2026-03-08`：收敛 `core/bridge` memory 的公开支持边界：在 `core/bridge/memory/doc.go` 与 `memory_config.go` 明确 Stable=`warm + cold/ledger + decision + graph + hygiene`、Experimental=`truth + vector + index + shadow`；`core/bridge/app` 公开 `Config` / `bridgeFileConfig` / `MEMORY_QUERY` 参数面已去掉实验 memory 开关，不再把 `include_vector` / `truth_debug` / `rerank_debug` 等字段当作公共 contract，`memory_ledger_dual_write` / `memory_ledger_shadow_compare` 也降级为内部兼容入口并在命中旧配置时打印 warning。`memoryManager` 启动日志现按 `[MEMORY][stable]` / `[MEMORY][experimental]` 分开打印，`docs/config.example.toml` 仅展示 stable 默认配置。新增 `memory_runtime_boundary_test.go` 与 `memory_manager_assemble_boundary_test.go` 回归，并已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app ./memory -run 'TestMemoryManagerConfigFromAppConfigDefaultsToStablePublicPath|TestMemoryManagerConfigFromAppConfigWarnsAndBridgesDeprecatedExperimentalFields|TestMemoryQueryParamsIgnoreExperimentalFields|TestConfigExampleOmitsExperimentalMemoryFields|TestNewMemoryManagerLogsStableAndExperimentalBoundaries|TestBusMemoryArchiveAndQuery|TestBusMemoryQueryReturnsGraphHits|TestBusMemoryQueryReturnsDecisionHits'` 验证。
- `2026-03-08`：为 `core/bridge/app` 落地 RSS Inbox 主链 MVP：新增独立 `RSSInboxStore` 快照存储（默认 `~/.ghost-os/rss/inbox.json`，含 `content_hash` / `dedupe_key` / `trace_id` / `source_feed_id`）、`RSSInboxService` 后台轮询与 AI 筛选流水线，以及 `RSS_INBOX_POLL` 系统任务接入。Bridge 启动时会按配置为启用状态自动创建全局统一轮询任务，复用共享 `FeedStore` 抓取启用源，按批次做 LLM 筛选并只把保留项写入 Inbox，同时用持久化 `seen_keys` 避免已见 feed item 重复处理。补齐 `/api/rss/inbox` 列表/过滤/手动轮询、`/api/rss/inbox/{id}` 查询与 `RSS_INBOX_LIST` / `RSS_INBOX_GET` / `RSS_INBOX_POLL` bus action，新增 `rss_inbox_path` / `rss_poll_enabled` / `rss_poll_interval` / `rss_poll_max_items_per_feed` / `rss_ai_batch_size` 配置项，并更新提示词明确 RSS inbox/poller 属于后台系统链路。新增 `rss_inbox_store_test.go` / `rss_inbox_service_test.go` / `transport_rss_inbox_test.go` 及配置回归，已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./tools -run '^$'` 验证。
- `2026-03-08`：为 `core/bridge` Agent 暴露统一 `task_manage` 工具：新增 `core/bridge/tools/task_manage.go`，支持 `create/update/delete/list/get` 五种 operation，首版只允许管理 `agent_message` 定时任务；`session_id` 可由 AI 显式填写，不填写则保持空值并沿用现有“触发时自动新建会话”语义。`core/bridge/app` 新增最小 task manager 适配层并按运行时是否具备 task store/scheduler 决定是否注册该工具，避免在无 Bridge task runtime 的场景暴露失效工具；同时补充 selector metadata 与 `task_manage_test.go` / `agent_runtime_factory_test.go` / `catalog_metadata_test.go` 回归。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./tools ./app -run 'TaskManage|TaskManageTool|AgentRuntimeFactory|CatalogMetadata'` 验证。
- `2026-03-08`：为 `apps/android` 落地会话历史列表 MVP：聊天页改为带侧滑抽屉的壳层，抽屉内可展示 `/api/sessions` 历史会话、切换会话与“新聊天”清空当前绑定；`BridgeClient` 新增 `listSessions()` / `getSession()` 只读接口，`ChatViewModel` 补齐 `sessions / sessionsLoading / sessionsError / historyLoading` 状态、`loadSessions()` / `selectSession()` / `createNewSession()` 行为，并把 `/api/sessions/{id}` 的历史消息压平成现有 Android 气泡消息，同时恢复未回答的 `ask_human` pending question。新增 `ChatViewModelTest.kt` 覆盖列表拉取、会话切换、`session_id` 持久化、pending 恢复与新聊天清空绑定；已通过 `./gradlew :app:testDebugUnitTest :app:assembleDebug` 验证。
- `2026-03-08`：修复 `ask_human` 选择答案后的续跑空消息问题：`core/bridge/agent` 的 `appendUserMessage(...)` 现在会跳过仅空白的 resume 输入，避免在 `AGENT_SEND(session_id only)` 续跑场景向模型追加空 `user` 消息，进而触发部分 provider 返回“消息为空或无效”的 `400`。新增 `TestRunWithPreloadedHistorySkipsEmptyResumeUserMessage` 回归测试，并已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./agent` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app -run 'TestQuestionAnswerEndpoint|TestSessionHistoryBuilder'` 验证。
- `2026-03-08`：为 Bridge/Android 打通最小“主动推到手机”链路：`core/bridge/app` 新增 session 级 SSE 推送 hub 与 `/api/sessions/{id}/events` 订阅端点，成功回合、`ask_human` 中断、取消结束消息都会按 `session_id` 广播，订阅建立时还会补发当前最早 pending question 快照；`apps/android` 新增 `SessionPushEvent` 模型与 `BridgeClient.observeSessionEvents(...)`，`ChatViewModel` 会在已配置 `baseUrl/token/session_id` 时自动重连订阅并把跨端消息/待回答提示落到聊天界面，同时 `ConfigScreen` 支持手动填写 `Session ID` 绑定已有会话。新增 `transport_session_events_test.go` 覆盖消息推送、`awaiting_human` 推送与 pending snapshot，并已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./app` 与 `./gradlew :app:assembleDebug` 验证。
- `2026-03-08`：将 `core/bridge/memory` 的 hygiene 读侧抑制统一收口为显式判定：新增 `ShouldInject(...)` / `ShouldReturnForQuery(...)` / `ApplyHygieneToEntry(...)` 与 `MemoryQuery` 的 `Explicit / Debug / AutoInject / IncludeQuarantine` 语义字段，并保留旧 `hygiene_mode` 兼容；`BuildContextWindow` 现按自动注入语义执行最终兜底过滤，`MEMORY_QUERY` 入口映射为显式查询，普通查询默认会隐藏 `votes >= 4` 项，`votes >= 6` 仅在 `include_quarantine=true` 时可见。新增 `hygiene_filter_test.go`，并扩展 `hygiene_read_filter_test.go` / `session_turn_preparer_hygiene_test.go`；已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory ./app` 验证。
- Web、CLI、Android 与 Bridge 的接口契约正在持续收口，跨端行为与数据结构相比早期版本已明显稳定。
- `core/bridge/app`、`core/bridge/tools`、`core/bridge/memory` 等核心模块已完成多轮拆分整理，职责边界与可维护性持续提升。
- 近期工作重点以“收口、统一、拆分、稳定”为主，说明项目已经从单点功能搭建阶段进入到系统化整理与增强阶段。
- `2026-03-08`：为 `core/bridge/memory` 新增独立 `hygiene` sidecar MVP：落地 `HygieneTarget / HygieneReason / HygieneRecord / HygieneAssessmentInput / HygieneStats` 模型、`records.json` 快照存储、固定垃圾评分规则（`votes * 0.2` capped 到 `1.0`）、`none/weak/strong/hard` quarantine 分级、reason 按 `Code` 去重合并，以及 `MemoryManager` façade 的 `GetHygiene / ScoreGarbage / HygieneStats / ListQuarantined`；默认路径为 `<memory root>/hygiene`，初始化采用 fail-open，不改写 truth/ledger。新增 `hygiene_store_test.go` / `hygiene_service_test.go` 覆盖累积评分、隔离升级、object/entry 主键分流、reason 合并、持久化恢复与 manager 无 truth/ledger 依赖场景，并已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory ./app` 验证。
- `2026-03-08`：完成 hygiene 第 3 步“读侧抑制”接入：`core/bridge/memory` 新增统一 `ShouldInject(...)` 读侧判定与 entry/candidate 过滤，对 `BuildContextWindow` 的自动注入启用严格模式（`votes >= 2` 过滤），对显式 `MEMORY_QUERY` 启用显式查询模式（`votes >= 4` 保留但打 `Metadata/Explain` 标记，`votes >= 6` 默认隐藏），且不改写 truth/ledger 底层存储。`core/bridge/app` 的 `session_turn_preparer` 自动 recall 已走同一 memory 层规则；同时收紧 planner hydration 作用域，避免非 hybrid shadow/debug 请求误改 live 结果。新增 `hygiene_read_filter_test.go` 与 `session_turn_preparer_hygiene_test.go` 覆盖 `votes=1/2/4/6` 阈值、显式查询/自动注入语义分流与 system recall message 回归，并已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./memory ./app` 验证。
- `2026-03-08`：为 `core/bridge/app` 的任务运行日志增加任务级保留上限：`TaskStore.AppendRunLog()` 在单任务目录写入成功后会于同一把锁内按 `StartedAt -> ScheduledAt` 排序裁剪，仅保留最近 `100` 条 `.json` 运行日志；裁剪失败仅写服务日志，不影响本次任务执行结果。新增 `task_store_test.go` 覆盖超额自动裁剪、不超额不删除、按最新时间保留、坏日志文件容错与 `ListRunLogs` limit 语义不变。已通过隔离 `task_store.go`/`task_store_test.go` 的最小 `go test` 校验；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app` 当前仍被既有 `app/service_usecase_memory_hygiene.go` 对 `MemoryManager.RunHygiene` 的编译错误阻塞。
- `2026-03-08`：增强 `core/bridge/app` 定时任务启动容错：`TaskStore` 新增严格/容错共用扫描路径与 `ListTasksTolerant()`，单个任务文件读失败、JSON 损坏、文件名/载荷 `id` 不匹配、配置非法时会按 `TaskLoadIssue` 记录并跳过；`TaskScheduler.Start()` 改为使用容错扫描，对损坏文件与单任务注册失败写入结构化 `log.Printf(...)`，同时保留目录不可读等基础设施错误的 fail-fast 语义，并修复启动失败后 `running` 状态未回滚的问题。新增 `task_store_test.go` / `task_scheduler_test.go` 覆盖坏 JSON、坏配置、注册期非法 cron、目录级故障边界；已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app` 验证。
- `2026-03-08`：为 `core/bridge` 新增只读 `rss_fetch` Agent tool：支持 HTTPS `RSS/Atom` feed 按需抓取、基础 SSRF 防护（拒绝 localhost/私网/非 HTTPS）、条目去重与按发布时间倒序归一化返回；已接入 runtime registry、tool selector metadata、默认 prompt 指引与 decision tool purpose 分类，并新增 `core/bridge/tools/rss_fetch_test.go` 覆盖 RSS/Atom 解析、摘要清洗、去重、max_items 与安全校验。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools ./context ./app ./memory/internal/decision` 验证。
- `2026-03-08`：为 `core/bridge` 增加 AI 自主管理 RSS 源的全局共享订阅层：新增文件快照型 `FeedStore`（默认路径 `~/.ghost-os/rss/feeds.json`）与 `feed_subscribe` / `feed_list` / `feed_update` / `feed_unsubscribe` 四个 Agent tools；`feed_subscribe` 会复用 `rss_fetch` 的 HTTPS/SSRF 校验并在入库前做一次真实 RSS/Atom 探测，订阅项支持 `title/tags/priority/enabled` 元数据、重复 URL 幂等更新与按启用态/优先级排序列出。已接入 runtime registry、tool selector metadata、默认 prompt 指引、decision tool purpose 与运行时配置加载链（`rss_feeds_path` / `GHOST_RSS_FEEDS_PATH`），并新增 `feed_registry_test.go` / `feed_tools_test.go` 与配置回归测试覆盖；在当前沙箱下已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go test -C core/bridge ./tools -run 'TestFeed|TestGetToolMetadata|TestFormatMetadataForSelector'` 以及 `go test -C core/bridge ./context ./app ./memory/internal/decision` 验证。
- `2026-03-08`：推进 `core/bridge/memory` QueryService 的 truth-first 主链切换：`memory_query_hybrid.go` 现改为固定四步 `planner -> truth primary -> projection hydration -> verify/rerank`；`QueryPrimary()` 产出的 truth primary candidate 成为 `Entries` 的主体来源，vector primary 命中改为 truth-backed entry；`hot/warm` 降级为上下文补充与 recency boost，`cold/ledger` 仅在 truth 主候选缺失或可回挂到已有 object 时作为原始事件证据补充，不再默认扁平化成独立事实层；并补充 `memory_hybrid_query_test.go` / `truth_shadow_test.go` 回归以覆盖 non-hybrid truth entry promotion 与 explicit truth query 行为。`gofmt` 已完成；当前工作树存在既有 `core/bridge/memory/internal/decision` 接口漂移，导致 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 仍被外部构建错误阻塞。
- `2026-03-08`：为 `core/bridge/memory` 落地真正的 `TruthReader.QueryPrimary()`：新增按 `subject / predicate / object / value / active_only / include_historical / expose_conflicts` 的 primary truth 查询入口，补齐 `claims_by_predicate`、增强版 `claims_by_subject_predicate`、`claims_by_value_lookup`、`objects_by_claim_id`、`objects_by_evidence_id` 读侧索引，并让 primary candidate 直接输出 `matched_claim_ids` / `matched_evidence_ids` / `conflicted_claim_ids`；保留既有 `TruthReader.Query()` 兼容路径，并新增 `core/bridge/memory/truth_query_primary_test.go` 覆盖默认 active-only 与 historical/conflict 查询。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 验证。
- `2026-03-07`：将 `core/bridge/agent/loop_test.go` 拆为 `loop_helpers_test.go` / `loop_run_test.go` / `loop_tool_test.go` / `loop_stream_test.go`，并将 `apps/web/lib/api.test.ts` 拆为 `api_agent.test.ts` / `api_config.test.ts` / `api_sessions.test.ts` + `api.test.helpers.ts`；已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./agent` 与 `pnpm --dir apps/web test -- --runTestsByPath lib/api_agent.test.ts lib/api_config.test.ts lib/api_sessions.test.ts` 验证。
- `2026-03-07`：继续收口测试结构语义：将 `core/bridge/app/transport_test_helpers_test.go`、`core/bridge/app/session_agent_runner_helpers_test.go`、`core/bridge/memory/memory_helpers_test.go` 统一迁入 `*_test.go` 语义域；把 `core/bridge/app` 的 bus / middleware / SSE 测试进一步按职责拆开；并将 Web API client 测试文件名对齐为 `apps/web/lib/api.agent.test.ts` / `apps/web/lib/api.config.test.ts` / `apps/web/lib/api.sessions.test.ts`。`pnpm --dir apps/web test -- lib/api.agent.test.ts lib/api.config.test.ts lib/api.sessions.test.ts` 已通过；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app ./memory` 仍被当前工作树中的 `core/bridge/memory` 接口漂移阻塞。
- `2026-03-07`：继续整理 `core/bridge/agent` 测试夹具：在 `loop_helpers_test.go` 中收口 `fakeCompleter` / `fakeToolCatalog` / `fakeTool` 的共享构造器与响应 builder（如 stop/tool_call/awaiting_human），并把 `loop_run_test.go`、`loop_stream_test.go`、`loop_tool_test.go` 的重复 fake/stub 装配改为复用 helper，降低单测样板噪音；已再次通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./agent` 验证。
- `2026-03-07`：记忆层已接入 deterministic intent planner、truth-object vector sidecar 与 shadow recall：`MemoryQuery` / `MemoryQueryResult` 新增 `intent_plan`、`vector_hits`、`shadow_report` 调试视图，truth shadow append/upsert 后会 best-effort 增量同步向量索引，并支持从 truth snapshot 全量重建；`QueryResultWithScope` 会在 live recall 完成后并行跑 planner/vector 仅做 debug/metrics，`BuildContextWindow` 与 Entries 排序保持冻结不变，`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory ./app` 已通过。
- `2026-03-07`：为 `apps/web` 增加暗色 `--ui-*` token 层与 `ui-*` 视觉原语，保持现有 `app.*` 色板不变；`ConfigPanel`、`ModelSelector`、`ChatInput`、`QuestionInput`、`MessageList`、`SessionSidebar`、`SessionItem`、`SessionList`、`app/page.tsx`、`app/error.tsx` 已切换到复用面板 / 输入 / 按钮 / hint / scrollbar 皮肤。`pnpm --dir apps/web test` 与顺序执行的 `pnpm --dir apps/web build` 已通过；并发执行时曾短暂触发 `/api/bus` collect page data 失败，顺序重跑后稳定通过。
- `2026-03-07`：启动 Web Chat Composer 第一阶段迁移：新增 `apps/web/components/ChatComposer.tsx`，以受控 React 组件重写 NextAI 风格的发送框外壳；`apps/web/components/ChatInput.tsx` 现改为仅承接业务 hint / draft / submit 状态，并通过 `apps/web/app/globals.css` 新增 `ui-composer-*` 样式实现内嵌发送按钮、底部工具区预留与 meta 行。当前仍只接现有 `sendMessage` 流程，尚未接入 stop / toolbar 业务项 / 附件能力。
- `2026-03-07`：完成 Web Chat Composer 第二阶段 stop 状态机：`apps/web/lib/api.ts` 新增 `stopAgent(sessionId?, traceId?)` 与客户端 trace id 生成；`apps/web/hooks/useBridgeChat.ts` 现跟踪 active run 的 `session_id/trace_id`、暴露 `stopCurrentRun` / `canStop`，并在用户主动停止时抑制 `agent run cancelled` 噪音错误；`apps/web/components/ChatComposer.tsx` 与 `apps/web/components/ChatInput.tsx` 已接入 send/stop 按钮切换和停止中禁用态。
- `2026-03-08`：完成 markdown→truth claim 投影视图第一阶段收口：`core/bridge/memory` 的 `MarkdownNode` 新增 `source_evidence_ids` / `source_claim_ids` / `derived_claim_ids` / `projection_version`（含 `projection_partial`），`SaveMarkdownNode` 切为 truth-first 再落 markdown projection，`LoadMarkdownNode` 与新增 `BackfillMarkdownLineage()` 会对历史节点做懒回填；`anchor_claim_mapper.go` 统一了 anchor→标准 claim（`preference.*` / `constraint.*` / `avoidance.*` / `identity.*` / `emotion.*`）以及 claim→anchor 投影；graph/decision 的 markdown 消费已改为优先认 claim-projected anchors，保留旧 `anchors/source_ids` 仅作兼容缓存。
- `2026-03-08`：为 `core/bridge/memory` 新增统一索引运行时 façade 与 `internal/indexer`：补齐按 ledger bucket 扫描、turn envelope 组装、projector 调度与 checkpoint 持久化；`MemoryManager` 已装配 `indexer` 并由 `StartDreaming` / `StopDreaming` 承接生命周期；新增 `IndexConfig`（`Enabled/Workers/BatchSize/PollInterval/CheckpointDir/ShadowCompare/LegacyColdAsync`）；graph 已可从 ledger 异步投影并在 runtime live owner 时切掉 `ArchiveToCold` 的同步 ingest，decision 已接入基于 ledger turn envelope 的 shadow projector；新增 `core/bridge/memory/index_runtime_test.go` 覆盖 graph live 投影、decision shadow 写入与 checkpoint 落盘，`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 已通过。
- `2026-03-07`：完成 Web Chat Composer 第三阶段 toolbar/meta 收口：新增 `apps/web/components/ComposerToolbar.tsx` 与 `apps/web/components/ComposerMetaRow.tsx`，把工具区 slot 与 meta 行从 `ChatComposer` 中抽离；`apps/web/components/ChatInput.tsx` 现提供默认安全状态 pill（Ready / Run active / Awaiting answer / Unavailable）并向 `ChatComposer` 透传 `toolbar` / `status` 视觉契约。
- `2026-03-07`：按当前范围收紧为“纯样式替换”：已移除 Chat Composer 的附件按钮 / 本地预览 / 拖拽文件处理 / `tokenEstimate` 契约，保留 React 壳体、toolbar/meta 视觉结构与 send/stop 状态切换。
- `2026-03-07`：继续只做视觉层收口：将 `apps/web/app/globals.css` 的 `ui-composer-*` 调整为更接近 NextAI 的圆角、内边距、焦点边框、底部 toolbar 贴边布局与更紧凑的 send/stop 按钮细节，同时保持 Ghost-OS 暗色 token 和现有 React 契约不变。
- `2026-03-07`：`ask_human` 契约升级为可选单选/多选结构化提问：Bridge schema/DTO 新增 `selection_mode` + `options`，要求最后一个选项支持自定义输入；Web Console 与 CLI 已支持选项选择、自定义补充与取消提问（取消后结束当前会话），历史投影也会保留结构化问题信息。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app ./agent ./session && go -C core/bridge test ./tools -run 'TestAskHuman'`、`cargo test --manifest-path apps/cli/Cargo.toml`、`pnpm --dir apps/web test -- --runInBand --runTestsByPath lib/api.agent.test.ts lib/api.sessions.test.ts lib/chatMessages.test.ts` 与 `pnpm --dir apps/web exec tsc --noEmit` 验证。
- `2026-03-07`：记忆层已接入 Week 3 的 truth live read + hybrid rerank：新增 `truth_query.go` / `truth_index.go` / `truth_hit.go` / `memory_candidate.go` / `memory_verify.go` / `memory_rerank.go`，`TruthWriter` sidecar 升级为 fanout，`QueryResultWithScope` 现可在灰度开关下把 hot/warm/cold/markdown/decision/graph/truth/vector 统一成 candidate，完成跨层去重、support/conflict 聚合、confidence 校准与 hybrid 排序；`MemoryEntry` / `MemoryQueryResult` 新增 `freshness` / `evidence_count` / `source_refs` / `truth_status` / `rerank_score` / `truth_hits` / `rerank_report`，`BuildContextWindow` 仅注入高置信、非冲突结果。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app ./memory` 验证。
- `2026-03-07`：记忆层已接入 Week 4 recipe reuse 闭环：在 `core/bridge/memory` 新增 recipe 选中 / advisory 注入 / `recipe_run` 执行跟踪 / 回合结束反馈回写 / 历史 backfill runner / 灰度默认与 kill switch，普通 hybrid rerank 仍保留为低置信回退；并将 `core/bridge/app` / `docs/config.example.toml` 补齐 `memory_decision_recipe_*` 配置透传。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory ./app` 验证。
- `2026-03-07`：按“仅视觉重建、不做能力拓展”的范围完成 `apps/web` Web Console 亮色重构：`apps/web/app/page.tsx` 已改为高相似 `NextAI` 的 topbar + sessions sidebar + chat panel 骨架，`apps/web/app/globals.css` 已从暗色 `ui-*` 布局切换为暖白亮色 token / panel / composer / settings 体系，`apps/web/components/SessionSidebar.tsx` / `SessionList.tsx` / `SessionItem.tsx` / `MessageList.tsx` / `QuestionInput.tsx` / `ChatComposer.tsx` / `ModelSelector.tsx` / `ConfigPanel.tsx` / `app/error.tsx` 已同步重做为亮色组件壳；未新增附件、toolbar 扩展、plan mode 或其他新能力入口。已通过 `pnpm --dir apps/web exec tsc --noEmit`、`pnpm --dir apps/web test` 与 `pnpm --dir apps/web build` 验证。
- `2026-03-07`：cold memory 已完成 step 1 的 ledger 迁移骨架：`core/bridge/memory` 新增 `LegacyColdStore` + `ColdMemory` façade、append-only `ledger` event schema/segment/manifest/replay/backfill/checkpoint、兼容 `Retrieve` / `ListSessions` / `ListArchives` 的 ledger replay、shadow compare 指标，以及 `core/bridge/app/session_turn_committer.go` 的每轮 dual-write；`core/bridge/app` 已补齐 `memory_ledger_*` 配置透传。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory ./app` 验证；`go -C core/bridge test ./memory ./memory/graph ./memory/decision ./app` 额外尝试时，`./memory/graph` 与 `./memory/decision` 子包仍受现有工作树中的包边界漂移影响未通过，本次未顺手修复。
- `2026-03-07`：完成 `core/bridge/memory` 第一阶段去重收口：确认根目录 `graph_*` / `decision_*` 为唯一真源，移除工作树中的历史副本目录 `core/bridge/memory/graph` 与 `core/bridge/memory/decision`，避免同属 `package memory` 的双份实现继续参与维护与编译发现。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge list ./memory`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge list ./... | rg '/memory/(graph|decision)$'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 验证。
- `2026-03-07`：启动 `core/bridge/memory` 第二阶段真边界收口：保留根包 `MemoryManager` façade 不变，将 graph 侧实现文件下沉到 `core/bridge/memory/internal/graph`，根包新增 `graph_service.go` 仅做装配与转换适配，避免子目录继续使用 `package memory` 伪拆分；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory/internal/graph` 已通过。decision 侧仍待按同一模式继续收口。
- `2026-03-08`：完成 `core/bridge/memory` 第二阶段真边界收口的 decision 部分：将 `decision_store/query/capture/reuse/feedback/recipe/backfill` 等实现下沉到 `core/bridge/memory/internal/decision`，根包新增 `decision_service.go` / `decision_recipe_query.go` 仅保留 façade、装配与少量 query 装饰；根包测试对 `manager.decision.store/distiller` 的直接访问已改为走 façade debug helper。补充 `MemoryConfig` 与 `NewTruthWriter` 的 legacy flat config 兼容映射后，`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory/internal/graph`、`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory/internal/decision` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge list ./memory ./memory/internal/graph ./memory/internal/decision` 已通过。
- `2026-03-08`：`core/bridge/memory` 完成去重收口 / 包边界整理的文档与护栏补强：更新 `core/bridge/memory/doc.go` 明确“根包 façade、`internal/*` 实现、`*_test.go` 回归区”的目录契约，并补上 `query` / `recipe reuse` / `truth shadow` / `graph recall` 的关键回归测试，锁住根包入口与 sidecar 实现分层后已稳定下来的行为。
- `2026-03-08`：完成 `core/bridge/tools` 第一轮收口清理：将分散的 `catalog_*` / `contract_*` 基础设施文件合并为 `catalog.go` 与 `contracts.go`，保留 `context_session.go` 负责会话上下文注入；同时删除 `read_and_summarize.go` 中重复的配置转换 helper，减少工具层样板与文件噪音。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools/...` 验证；额外尝试 `go -C core/bridge test ./app ./agent` 时，`./app` 仍被当前工作树中的 `memory/memory_types.go` 缺失 `BucketPlan` / `BucketShadowReport` 定义所阻塞，本次未顺手修复。
- `2026-03-08`：继续整理 `core/bridge/tools` 第二轮结构：将 `web_search` 的 DuckDuckGo HTML 解析与结果清洗下沉到 `core/bridge/tools/internal/websearch/parser.go`，顶层 `web_search.go` 仅保留请求编排；同时压缩 `read_and_summarize` 运行态字段，只保留实际需要的 `maxFiles/defaultMaxChunks/service/setupErr`，并为 `script_exec` 补上 `payloadutil.AnySlice` 以统一数组 payload 校验。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools/...` 验证。
- `2026-03-08`：完成 `core/bridge/memory` 第三步“压缩超大文件和配置面”：`MemoryConfig` 现按 `Warm` / `Cold` / `Ledger` / `Recall` / `Truth` / `Graph` / `Decision` / `Vector` / `Runtime` 小配置块组织，`MemoryManager` 拆为 `memory_manager.go` / `memory_manager_assemble.go` / `memory_manager_api.go`，仅保留 façade 与装配；`memory_query_hybrid.go` 的 candidate/merge/hydrate helper 已迁到 `memory_query_hybrid_helpers.go`，`truth_store.go` 的 snapshot/state/io/shadow helper 已迁到 `truth_store_state.go` / `truth_store_io.go`，进一步把单文件压回到单一核心职责。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'TestLoadConfigWithRuntime_LoadsDecisionRecipeReuseSettingsFromEnv|TestLoadConfigWithRuntime_LoadsDecisionRecipeReuseSettingsFromFile|TestSessionTurnCommitter'` 验证。
- `2026-03-08`：为 `apps/web` 聊天输入区补上内嵌激活模型选择器：`apps/web/hooks/useBridgeConfig.ts` 现同步拉取 provider 列表并生成跨供应商模型选项，选择项会通过一次配置更新同时切换 `active_provider + model`；`apps/web/components/ModelSelector.tsx` 改为 NextAI 风格的上拉面板，挂载到 `apps/web/components/ChatInput.tsx` 左下角工具区，并在 `apps/web/app/globals.css` 增加对应 trigger / panel / option 皮肤。已通过 `pnpm --dir apps/web lint`、`pnpm --dir apps/web exec tsc --noEmit --pretty false` 与 `pnpm --dir apps/web build` 验证。
- `2026-03-08`：继续微调 `apps/web` 模型选择器触发器样式：去掉输入框左下角选择器的底座/胶囊外壳，只保留纯文字模型名与箭头；同时移除触发器外层的 provider 名展示，保留上拉面板内按供应商分组的选项结构。
- `2026-03-08`：继续修正 `apps/web` 模型选择器触发器的文字/箭头基线：将触发器改为 baseline 对齐，并上提小三角，减少模型名与箭头的垂直偏差。
- `2026-03-08`：继续收口 `apps/web` 模型选择器弹层文案：移除上拉面板顶部 `Active Model / 选择供应商模型` 标题区，只保留模型选项列表，减少输入区视觉噪音。
- `2026-03-08`：继续精简 `apps/web` 模型选择器列表项信息层级：移除每个模型项左下角重复的 provider 副文案，仅保留分组标题中的供应商名称，避免面板内重复标注。
- `2026-03-08`：继续收口 `apps/web` 模型选择器列表样式：移除模型项卡片式底座、边框和悬浮背景，改为更轻的纯文本列表，仅保留文字高亮与选中点。
- `2026-03-08`：继续微调 `apps/web` 模型选择器弹层尺寸：保持左边缘对齐触发器，但将上拉面板宽度放宽为不小于 240px（移动端 220px），避免纯文字触发器过窄时影响模型列表可读性。
- `2026-03-08`：完成 `core/bridge/tools` 第三轮目录收口：将 `context_session.go` 并入 `contracts.go`，把分散的原子文件工具合并为 `file_tools.go`、执行类工具合并为 `exec_tools.go`，并移除 `web_search.go` 中仅作中转的本地结果类型别名；同步更新 `doc.go` 目录说明，进一步压缩 `tools` 顶层文件噪音并保持公共契约集中。`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./tools/...` 已验证。
- `2026-03-08`：完成 `core/bridge/memory` 第四步 bucket-aware recall 基线：新增 `bucket_manifest.go` / `bucket_plan.go`，把 ledger manifest 扩展为 session stats、top terms、kinds、turn/trace 聚合，并在 `memory_query_hybrid.go` 中于 intent plan 之后插入 bucket planner；`MemoryQuery` / `MemoryQueryResult` 新增 namespace/workspace/session/month hints、`BucketPlan`、`LayerFreshness`、`ShadowRead` 调试面，cold/markdown/vector/truth 查询已接入 bucket 过滤或 plan-aware 读取；graph/decision projector 可输出 `view_manifest.json` 供后续 freshness 粗筛；同时修复 decision façade 在根包/`internal/decision` 之间转换时丢失 `NewMessages` / `RecentHistory` 的问题，确保 turn-capture 能正确保留 tool use 与 `ask_human` 证据。已通过 `go -C core/bridge test ./app ./memory` 验证。
- `2026-03-08`：将 `apps/android` 从硬编码深色 UI 切换为统一白色主题：新增 `apps/android/app/src/main/java/dev/ghostos/android/ui/theme/Theme.kt` 浅色 `MaterialTheme` token，`MainActivity.kt` 统一包裹 `GhostOSTheme`，并将 `ChatScreen.kt` / `ConfigScreen.kt` 的 `Scaffold`、`TopAppBar`、输入框与消息气泡改为复用浅色 `colorScheme`；同时把 `apps/android/app/src/main/res/values/colors.xml` 的启动图标背景切为白底深字，保留蓝色主操作色。已通过 `./gradlew :app:assembleDebug` 验证。
- `2026-03-08`：对 `apps/web/app/globals.css` 做无视觉变更的样式拆分：将全局主题/reset/动画保留在 `app/globals.css`，并新增 `app/styles/primitives.css`、`shell.css`、`messages.css`、`composer.css`、`settings.css`、`responsive.css` 承接通用原语、页面壳层、消息区、输入区、设置弹窗与响应式规则；`app/layout.tsx` 改为显式导入这些分文件，降低单文件膨胀并保持现有类名契约不变。
- `2026-03-08`：完成 `core/bridge/memory` truth v2 第一段落地：新增 `truth_schema_v2.go` / `truth_event_types.go` / `truth_claim_status.go` / `truth_predicate_policy.go` / `TRUTH_SCHEMA_V2.md`，将 `MemoryClaim` 升级为独立 `subject/predicate/object/value/status` schema，并把 writer/replay 改为 `evidence.*`、`claim.asserted`、`claim.status.changed`、`object.projected` 事件流；`archive/markdown/decision` mapper 已开始 dual-write 标准 evidence/claim event，replay/verifier 可重建并校验 active/conflicted/superseded claim 集，新增 `truth_event_types_test.go`、`truth_predicate_policy_test.go`、`truth_claim_id_test.go`、`truth_arbitration_test.go`、`truth_replay_v2_test.go`。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'TestLoadConfigWithRuntime_LoadsDecisionRecipeReuseSettingsFromEnv|TestLoadConfigWithRuntime_LoadsDecisionRecipeReuseSettingsFromFile|TestSessionTurnCommitter'` 验证。
- `2026-03-08`：为 `core/bridge/memory` 的 decision sidecar 冻结 lineage 主链：`DecisionMemo` / `DecisionRecipe` / `RecipeRun` / `RecipeSelectionReport` 新增 `DecisionLineage` 字段族与 explain 结构，`decision_capture.go` 改为先采集 `source_event_ids` / `source_evidence_ids` 再产 memo，`decision_recipe.go` 改为基于 memo lineage 聚合 `source_claim_ids` / `source_evidence_ids` / `contradicted_claim_ids`，`decision_reuse.go` 新增 matched/missing/conflicted claim explain，`decision_feedback.go` 回灌 `selection_*` / `execution_evidence_ids` / `emitted_claim_ids` / `invalidated_claim_ids`，`decision_store.go` 增加 claim/evidence/memo/recipe/run 反查索引与 `ExplainMemoLineage` / `ExplainRecipeLineage` / `ExplainRecipeRunLineage` debug API，并补了 `TestDecisionLineageExplainAPIs`。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 验证。
- `2026-03-08`：补充 `core/bridge/memory/TRUTH_QUERY_CUTOVER_ROADMAP.md`，将 truth-first query 主路径切换整理为统一实施路线图：明确当前 legacy 归并基线、truth/markdown/decision/graph 的分层 cutover 顺序、所需新索引、灰度开关、回归矩阵与 PR 拆分；本次仅落文档与计划，不变更运行时行为，用于后续分批提交 truth-first candidate pipeline 与 projection hydration 改造。
- `2026-03-08`：落地 `core/bridge/memory` truth-first cutover 的 Phase 0 适配层：新增 `truth_query_cutover_types.go`，冻结 `TruthQueryOptions` / `TruthQueryExplain` / `PrimaryCandidate` / `ProjectionHit` 四个内部 DTO，并在 `memory_query_hybrid.go` 的 debug 面接入 truth query options、primary candidates 与 projection hits 的最小适配；同时为 projection hydration 预留 `projectionHitFromEntry()` 与 `primaryCandidateFromTruthMatch()` 适配函数，保持 `MemoryEntry` API 不变。新增 `truth_query_cutover_types_test.go` 覆盖 primary/projection DTO 归一逻辑；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory -run 'TestPrimaryCandidateFromTruthMatch|TestProjectionHitFromEntry'` 已通过。
- `2026-03-08`：收口 `core/bridge/memory` 查询主链并开始清理迁移护栏：删除 `memory_query_week2.go` / `shadow_recall.go` 的双轨查询分支，`QueryResultWithScope` 统一改走一条 recall 主链；`ColdMemory` 线上冷读改为 ledger 主账本优先，legacy 仅继续承担 archive 兼容写入与 backfill/import；`MemoryQueryResult` 缩回稳定面，仅保留 `entries + selected_recipe + recipe_advisory + recipe_selection`，原 `graph_hits / decision_hits / intent_plan / bucket_plan / layer_freshness / vector_hits / truth_hits / rerank_report` 降为独立 `debug` 视图，并同步调整 `core/bridge/app` 的 `MEMORY_QUERY` 响应；同时在非 hybrid 模式下保留 week2 输出语义（BuildContextWindow / decision recipe / shadow truth 回归保持稳定），而 hybrid 模式继续走统一 `candidate -> verify -> rerank` 主链。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory ./app` 验证。
- `2026-03-08`：完成 `core/bridge/memory` truth-first cutover 的 Phase 5（decision claim-query）：`internal/decision/decision_query.go` 的 memo/recipe 候选入口改为优先走 `memoIDsByClaimID` / `recipeIDsByClaimID` / `runIDsByClaimID`，不再用 `memoIDsByIntentKey` / `memosByAnchorKey` / `memosByGraphNode` 作为主召回入口；`DecisionHit` / decision projection explain 补齐 `object_id`、`source_claim_ids`、`source_evidence_ids`、`missing_claim_ids`、`conflicted_claim_ids`，并保留 `anchor_keys` / `graph_refs` 仅作 explain 与兼容输出；新增 `memory_query_decision_test.go` 覆盖 claim-first memo/recipe 命中与 run-claim 反推 recipe 的回归。已通过 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory -run 'TestDecisionRecallPrefersClaimLineageCandidates|TestDecisionRecallUsesRunClaimIndexForRecipeCandidates|TestMemoryManagerQueryResultIncludesDecisionHits|TestBuildContextWindowInjectsConciseDecisionRecall|TestBuildContextWindowDecisionRecallSkipsDuplicateSummary'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'TestBusMemoryQueryReturnsDecisionHits'` 验证。
- `2026-03-08`：推进 `core/bridge/memory` truth-first cutover 的 Phase 6（收敛去重与 hydration）：`memory_query_hybrid_helpers.go` 现移除 `memo_id` / `node_id` / `source_ids` 参与主链 hydration 的兜底；`objectIDFromEntry()` 改为优先按 `object_id` 与 claim/evidence lineage 解析 object，`recallCandidateKey()` 改为仅按 `object:<id>` 或 `projection:<type>:<id>` 去重，`source_ids` 仅保留兼容 metadata；并新增 `memory_query_hybrid_helpers_test.go` 覆盖 primary/projection key、archive fallback 与 lineage object 解析。尝试执行 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory -run 'TestRecallCandidateKeyPrefersObjectAndProjectionRules|TestEntrySourceRefsForHydrationKeepsLegacyFallbackOnlyForArchive|TestObjectIDFromEntryPrefersClaimAndEvidenceLineageBeforeFallback|TestPrimaryCandidateFromTruthMatch|TestProjectionHitFromEntry|TestTruthReaderQueryPrimary'`，但仍被当前工作树中既有 `core/bridge/memory/internal/graph/graph_format.go` 与其本地 `MemoryEntry` 契约不匹配（`unknown field Explain`）阻塞。
- `2026-03-08`：完成 `core/bridge/memory` truth-first cutover 的 Phase 7（planner 归位）：`memory_intent.go` 现将 planner 从“补 query terms”升级为“选记忆层”，新增 `procedural-first / semantic-first / self-first / episodic-first / mixed` 五种 recall mode，并直接产出 `QueryIntentPlan.Truth` 与 `Hydration`；`truth_query_cutover_types.go` 改为优先用 planner 产出的 truth options，再由显式 query metadata 覆盖；`memory_query_hybrid.go` 则按 planner 选择的 hydration scope 只打开对应层（如 procedural 只放行 `decision/warm`，semantic 只放行 `markdown/graph`，episodic 只放行 `hot/warm/cold`），不再默认把所有层都打开。补充 `memory_intent_test.go` 与 `memory_planner_scope_test.go` 回归，并通过 `go test -C core/bridge ./memory -run 'TestIntentPlannerParsesFiveFacets|TestIntentPlannerPrefersScopeEnvironment|TestIntentPlannerSelectsRecallModes|TestPlannerHydrationScopeNarrowsEnabledLayers|TestTruthReaderDebugHitsMatchFacets|TestHybridQueryPromotesVectorHitsThroughTruth|TestHybridBuildContextWindowSkipsCandidateEntries'` 验证。
- `2026-03-08`：为 `core/bridge` 增加最小后端定时文本任务能力：新增 `TASK_CREATE` / `TASK_LIST` / `TASK_GET` / `TASK_DELETE` bus action 与 `/api/tasks` / `/api/tasks/{id}` HTTP 接口；任务定义与运行日志独立持久化到 `~/.ghost-os/tasks/{tasks,logs}`，支持 `interval_seconds` 重复执行与 `cron_expr` 重复调度，并统一复用现有 `AGENT_SEND` 主链。填写 `session_id` 时要求目标会话存在且未结束；不填写时每次触发都以空 `session_id` 执行，由现有会话主链自动创建新对话。新增 `core/bridge/app/transport_tasks_test.go` 覆盖 CRUD、bus 接口、调度执行、空会话新建语义与重入跳过；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app` 已通过。
- `2026-03-08`：补齐 `core/bridge` 定时任务管理缺口：新增 `TASK_UPDATE` / `TASK_RUN_NOW` / `TASK_LOGS` bus action，以及 `PATCH /api/tasks/{id}`、`POST /api/tasks/{id}/run`、`GET /api/tasks/{id}/logs` HTTP 接口；`enabled=false` 现可真实持久化并停止调度，手动执行可复用同一任务定义并返回运行结果，任务运行历史支持按时间倒序读取与 `limit` 截断。`TaskScheduler.Stop()` / `Unregister()` 现会等待调度循环与在途执行 goroutine 完整收尾，避免测试临时目录清理时仍有后台写入；测试夹具也补上 `service.Close()` 清理。新增 `transport_tasks_test.go` 覆盖 update/disable、run-now、logs、bus 管理动作与 Stop 等待收尾语义；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'Task|Tasks'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app` 均已通过。
- `2026-03-08`：将 `core/bridge` 定时任务升级为双模式：`agent_message` 继续复用 `AGENT_SEND`，`system_action` 新增首个内部动作 `MEMORY_HYGIENE_RUN`；任务模型/DTO/运行日志现补齐 `task_kind`、`action`、`action_params`，老任务缺省回落到 `agent_message`。调度器已按任务类型分发，`MEMORY_HYGIENE_RUN` 可被 `/api/bus` 手动调用，也可由定时任务直连 `memoryManager.RunHygiene()` 执行，不再经过 Agent。`core/bridge/memory` 新增 deterministic hygiene runner：支持 `warm|projection|report` 三种 scope、`limit / dry_run / min_confidence / max_votes_per_run / reason_codes` 参数、保守候选扫描与评分、每次命中只写 hygiene sidecar（不改 truth/ledger），并额外落地 `scores/*.json` 评分日志以记录 `trace_id / task_id / scope / target / vote_delta / reasons`。新增 `transport_tasks_test.go` / `hygiene_runner_test.go` 覆盖 system_action 创建、非法 action 校验、bus 手动触发、调度执行写日志、dry-run 不落盘、scope 行为与评分日志写入；`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'Task|Tasks|MemoryHygiene'` 与 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory -run 'RunHygiene|Hygiene'` 已通过。当前 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app ./memory` 仍被既有 `TestShadowRecallKeepsLiveOutputStable` 失败阻塞，与本次任务无直接改动耦合。
- `2026-03-08`：为 `core/bridge/app` 的任务调度补齐运行级取消控制：`TaskScheduler` 现为每次任务执行创建独立 run context，默认附带 `2m` 执行上限；`Stop()`、`Unregister()` 与注册替换旧任务时会同时取消调度 loop 与当前 run；`executeRun()` 现将 `context.Canceled` 归一为 `cancelled`，将超时归一为带明确文案的 `error`，并把结果回写到 `TaskRunLog` / `LastError`。新增 `task_scheduler_test.go` 覆盖 Stop 取消、RunNow 超时、Unregister 取消、普通错误保持为 error，并将 `transport_tasks_test.go` 的 Stop 语义升级为“取消运行中任务”。尝试执行 `GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./app -run 'TestTaskScheduler(StopCancelsRunningTask|StopCancelsRunningTaskUnit|RunMarksTimeout|UnregisterCancelsRunningTask|RunNowHonorsTimeout|RunKeepsNormalErrorsAsError|UsesProvidedSessionAndLogsRun|LeavesSessionBlankToOpenNewConversation|SkipsConcurrentRun)'`，但被当前工作树既有 `core/bridge/app/service_usecase_memory_hygiene.go` 对 `MemoryManager.RunHygiene` 的缺失引用阻塞，暂未完成包级验证。
- `2026-03-08`：完成 `core/bridge/memory` truth-first cutover 的 Phase 5（graph explain projection）：`internal/graph` 的 `GraphHit` / `GraphEdge` 补齐 `object_id`、`source_claim_ids`、`source_evidence_ids`、`explain` / `metadata`，并让 markdown 文本抽取出的 graph fact 继承 truth projection lineage；`graph_query.go` 改为优先按 `claim overlap`、`claim alias`、`object lineage` 做 seed / traversal，legacy graph 命中会显式标注 `fallback_kind=legacy_graph` 与 `fallback_not_truth_derived=true`；`memory_query_hybrid.go` / `memory_query_hybrid_helpers.go` 则把 graph 限定为 explain 视角，先做 truth primary，再用 truth object / claim / evidence 回填 graph hit 的 `object_id` 与 lineage，不再把未 truth-backed 的 graph 命中当作独立事实源。补充 `internal/graph/graph_query_test.go` 与 `memory_graph_recall_test.go` 回归，`GOCACHE=/tmp/go-build GOTMPDIR=/tmp/go-tmp go -C core/bridge test ./memory` 已通过。

- `2026-03-08`：放宽 `drivers/native` 默认沙箱文件白名单，不再只限当前工作目录；`SandboxConfig::default()` 现会自动纳入当前 `cwd`、用户家目录以及 `/media/<user>/{Files,Apps,App}` 中实际存在的挂载目录，方便 Agent 直接访问 `/home/ruan` 与已挂载的数据盘，同时继续保留 `*.env` / `*id_rsa*` / `*credentials*` 的敏感文件阻断。补充 `sandbox::config_test` 回归覆盖路径去重与挂载发现，并已通过 `cargo test --manifest-path drivers/native/Cargo.toml sandbox` 验证。
