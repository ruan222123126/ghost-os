# Ghost-OS 项目进展（摘要版）
更新日期：2026-05-03
当前阶段：MVP 稳定化（主链路可用，持续收口）
文档规则：仅保留阶段结论，`总行数 <= 30`。

## 总览
- 三层架构稳定：Execution（`drivers/native`）/ Central（`core/bridge`）/ Perception（`apps/web`、`apps/cli`、`apps/android`）。
- 当前主开发中心为 `core/bridge`，目标是稳定性、可观测性、契约一致性。
- Assistant 工具调用基线：`<t:ID>JSON</t>` + `[TOOL_TAG_RESULT]`。
- 协作基线：可并行即优先多代理；跨进程交互统一 `trace_id` 可追踪。

## 分层状态
- Central：会话、工具调度、SSE、任务、配置主链路可用；`ServiceResult` 契约已落地，transport 仅负责 HTTP 映射；`tools/contracts` 已去除对 `session.Session` 的直接依赖；工具执行器已补调用前 `ctx.Done` 快速失败与 tool_call 参数摘要的调试开关守卫；流契约已扩展 `completion_delta.kind=thinking` 并覆盖 codex/anthropic/openai(custom) reasoning 流转，OpenAI 兼容 provider 在工具往返后现会透传 assistant `reasoning_content` 以支持 thinking 模式续跑，长会话续聊所需上下文窗口现优先走单供应商配置里的 `context_window_tokens`，且会话热窗口加载、请求前裁剪与 `microcompact` 现都会保留完整 reasoning/tool user-turn、避免把 thinking replay 链路从中间裁坏；会话历史构建现会在请求前清理孤儿 `tool` / 损坏 tool-call span，避免旧坏 session 因未知 `tool_call_id` 卡死续聊；agent 失败回合不再提前提交用户消息到主历史，而流式失败/取消仍会由 orchestration 补存本轮 user + assistant draft；空消息续跑现仅允许明确的人类回答恢复场景，避免误触发无定义 resume 把历史写乱；本地 reasoning replay 校验现已覆盖 tool-loop 结束后的最终 assistant 文本，避免 DeepSeek 类 thinking provider 把缺失 `reasoning_content` 的坏历史落盘并在下一轮续聊时报 400，且流式失败回合现在也会保留本轮 user message 与 assistant draft 落盘；runtime system prompt 现会复用同轮技能发现结果，并为 `SKILL.md` 发现缓存与 system prompt 文件读取增加文件签名失效，避免重复读盘同时确保提示词刷新能感知技能/提示词文件变更；`tool_call_started/finished` 现新增 `arguments_json/output` 以便前端即时可读化展示；复杂函数与超长文件持续拆分中。
- Perception：Web/CLI 已可用并接通流式与用户图片输入；Web 聊天状态和 API 解析链路已收敛并补测，新增 streaming thinking 面板（正文首 token 自动折叠、可手动展开/收起、结束清理），tool 卡片现会在工具成功但 agent loop 继续时继续保留参数标题（不再退回成工具名），流式 terminal error 在拿到 `session_id` 后也会立即回读最近历史，避免失败回合看起来整段消失；General 现可配置非 CLI 默认 `project_root`，CLI 则会在每次 agent 请求里显式上送启动目录作为请求级覆盖，消息区虚拟列表行测量已改为稳定 ref 以避免 `measureElement` 更新循环，消息区滚动跟随现已拆到独立 hook/布局签名状态机，streaming thinking/assistant/tool 增量仅在回到底部阈值内时自动跟随，并通过 virtualizer rAF ResizeObserver 合并与禁用 `overflow-anchor` 减少抖动；Android 已接入会话与展示基础，成熟度低于 Web/CLI。
- Execution：截图、输入、脚本、窗口查询等原子能力可用；`CODEX_CLI` 已回归 native 原子 action；Wayland 截图已接入 ScreenCast+PipeWire 持续会话（`OpenPipeWireRemote` + `pipewiresrc` 单帧抓取），减少识别链路反复截图造成的暗屏；bridge/native 启动与首次截图日志现在会显式输出 persistent 配置、session 类型与 capture backend，便于定位“代码已改但实际仍跑在 X11”这类问题；native 关键路径测试与 CI 矩阵已补齐，`task.py` 的 `ping/agent/serve/build` 现在默认以 `python-sandbox` 特性编译 native，并通过 `GHOST_NATIVE_BINARY_PATH_OVERRIDE` 显式覆盖文件配置里的 `native_binary_path`，避免脚本链路误回退到旧 release native 导致 `SCRIPT_EXEC unavailable` 或沙箱能力不一致，但整体仍未生产完备。

## 当前可用能力
- 会话：创建、流式输出、持久化、恢复、分页历史、草稿态展示；删除会话时会同步删除人类可读 markdown 日志（含孤儿日志清理）；用户 stop 会等待 run 退出并保留中断 assistant 草稿，Web 随后回读最近历史。
- 工具：`list_files`、`read_file`、`search_files`、`write_file`、`apply_diff`、`bash_exec`、`script_exec`、`screen_action`、`screen_control`、`web_search`、`sfind` 可运行，且本次拆分后 `script_exec` 行为保持不变；`sfind` 已取代 `tfind` 并收敛为 skill-only 动态加载器，仅搜索/加载/卸载/列出 `SKILL.md` skill，不再动态加载 tools 或 skill 依赖 tools；runtime prompt 现新增静态 `Skills:` 目录与动态 `Skill Context:` 正文区，并与 `sfind` 共用同一套可见 skill 解析（支持 `skill_blocklist`、repo/user 同名去重、repo 优先），同一 run 内 `sfind(load/unload)` 后下一次 completion 会自动刷新 system prompt，Web tool detail/compact step 现会直接显示 `sfind` 的 `load/unload/list/search` 动作与 skill 名称，不再退化为通用 `run`；`script_exec` 公开脚本 API 已硬切为裸函数 `list_files/read_file/search_files/write_file/apply_diff/bash_exec/fetch_webpage`，不再公开 `tools` 对象，旧 `tools.*` 仅在 Web 历史日志摘要解析中保留展示兼容；顶层 helper 现兼容位置参数/命名参数，且 prompt/description 已补充 `list_files` 返回字符串数组这一真实契约，`write_file` 现会自动创建缺失父目录，`bash_exec` 现支持显式 `max_output_chars` 覆盖默认 stdout 截断预算；native sandbox 现额外提供受限 text-only `open(path, mode)` shim（仅 `r/w/a` + UTF-8，其他模式显式报错），并预注入 `json/os/sys`、将 `subprocess.run/check_output` 与 `os.popen/os.system` 收敛为走受控 shell shim；原 system `Tool Guidance` 已下沉到各工具默认 prompt，并会在加载时自动迁移旧默认 prompt 文件；`/api/prompts/system` 预览中的 `tool_definitions` 与真实 `CompletionRequest.Tools` 现已统一收敛到同一套简化 description/schema；`web_rooter` 与旧任务管理工具已从 bridge 工具目录、运行时注册、提示引导、共享契约与 Web 运行时设置链路中移除；`text_input` 已合并进 `screen_control action=text_input` 并从独立工具目录移除；`read_and_summarize` 已从运行时与契约移除；`screen_control` 现通过独立无 `cwd` execution client 运行，避免切换或失效 `project_root` 时 `find_icon`/截图预览被 `SCREEN_CAPTURE chdir` 误伤；`screen_action` 已为 OCR/icon 链路增加短 TTL 截图复用（`reuse_cache`/`cache_ttl_ms`），降低高频识别场景重复截图带来的闪屏感。
- 任务：`agent_message` 与 `workflow` 基线可用，支持创建、查询、调度、手动执行；Web bridge 已修复无 body POST 透传，任务“运行”按钮不再误报 `invalid JSON body`；运行日志已扩展 `node_results`（节点输入/输出快照、开始/结束时间、并发完成序 `completed_seq` 与 `branch_id`），前端解析已兼容历史 run 的 `node_results` 缺失/`null`。
- Agent：支持后端 `mode=plan`（仅输出编排文本，禁止工具与任务执行）。
- Workflow：`if/loop` 运行时与画布编辑可用；工具节点按 `input_schema` 固定参数，workflow 变量输入器已接入 `llm/agent/tool if value` 可模板字段；支持节点复制与连线编辑；`screen_control` 已收敛为 atomic-only，屏幕编排动作已移除 `ocr_scan`、将 `click_text` 收敛为 `find_text`、并支持 `click` 坐标编辑后直连原子点击；`find_icon` 支持模板上传与即时识别预览，预览请求默认 `max_results=1`，native 匹配在 single-result 路径并行化+提前剪枝，缓解“检测中”长时间卡住；`hover_after_match` 现在会在前端测试与工作流运行时都透传为真实 `MOUSE_MOVE` 悬停，若 native 产物过旧则显式提示重编 `drivers/native`；运行时仍支持 `workflow_template_data_url` 自动落盘并输出 `exists/match_count/primary_match` 便于流程判断；Workflow `screen_control` 现新增 `workflow_steps`（多步顺序执行、失败即停、与 `action/params` 共存时报错，默认在相邻步骤间加入 `200~300ms` 随机延迟），前端编排器已改为 0/1/多步分支同步并支持从任务定义回填步骤队列；画布 loop start/end 语义已收敛为“end 仅保留退出边”，不再要求显式 `end -> start` 回边。
- Web 配置：General / Tasks / Skills / Tools / Presets 页面可用；设置导航已移除“偏好”分组并新增“提示词”分组；提示词设置已拆分为“库 / 预览”双页（兼容 `settings=prompts` 自动落到库页），继续复用独立 `/api/prompts/system` 接口，并新增独立 `/api/presets` CRUD/activate：预设按严格 `tool_allowlist` + `prompt_refs(rule/core_job/memory/context[])` 管理、仅持久化提示词卡 ID 引用，现支持一键激活并同步当前严格工具 allowlist、`tool_allowlist_only` 与提示词卡激活顺序；库页已改为提示词卡片列表 + 点击弹层编辑（`name`/`insert_point`/`content`/`active`），新建/编辑时插入点现收敛为 `rule/core_job/context`，其中 `rule/core_job` 继续单激活、`context` 支持多张同时激活并按保存顺序渲染，legacy `memory` 卡片仅保留兼容显示且不再插入 system prompt，Preset 编辑器也已支持按勾选顺序绑定多张 context 卡片；默认系统提示词已收敛为单版本扁平模板（`Role/Job/Skills/Skill Context/Context`），后端持久化 `prompt_library` 并由激活 `core_job` 卡片反向编译 `core_prompt`，预览页现以单一只读块连续展示完整渲染提示词与当前生效工具定义 JSON，且“库 / 预览”已保持与其他设置页一致的左右宽度；`global_template` / `tool_prompt` / `tool_key_spec` 已从 system prompt 存储、API 契约与运行时链路移除，legacy prompt 文件会在加载时自动清理，且已补 UI/Hook/API/parser 测试；Tools 仍只负责每个工具的 prompt 覆盖与启停；会话侧栏“历史分组开关”在开关两种模式下均统一按最近活跃时间降序展示（分区顺序保持不变，分区内忽略拖拽顺序，拖拽仅改变分区归属）；分区右键菜单已支持“新建/重命名/删除”（删除后会话回收到未分类）；会话分区已改为后端持久化，旧 `localStorage` 仅用于一次性迁移，刷新页面或重开 Web/bridge 后仍保留归属；General 运行时设置现已改为字段变更防抖热更新并移除底部“保存/重新加载”按钮，页首“界面语言 / 历史会话分组”也已统一切到与其余运行时项一致的连线式 section，新增 `microcompact_enabled`、`max_turns`、`llm_completion_retry_count` 与 `llm_completion_retry_interval_ms` 热更新项，其中 LLM completion 瞬时错误与 thinking/reasoning replay 协议错误现已共享同一套可配置自动重试并持久化到 `config.toml`；`chat_path` 清空后会正确回退 provider 默认路径、`/api/config` 同步返回覆盖值语义，General 不再暴露供应商 `API Key/base_url`，供应商页现可直接编辑单供应商 `context_window_tokens`，编辑时留空 `API Key` 会保留已保存值；运行时设置中的 GraphQL 文本工具调用协议相关开关与配置链路已移除；Workflow 工具参数编辑对 `screen_control` 隐藏 `mode` 与顶层 `display_id`，保留必要输入键，`click` 步骤编辑弹窗已支持“获取鼠标位置”取点（轮询当前坐标、Enter 确认、Esc 取消，并回写 `display_id` 以对齐原子点击缩放），相对坐标模式现会在运行时先读取当前鼠标位置再叠加偏移，避免误用屏幕绝对坐标；屏幕编排步骤的追加/移动/删除/编辑现会统一同步到 `screen_control` 参数，删除 `click` 步骤后不再执行遗留 `click_icon`；`find_icon` 步骤编辑弹窗已切换为新版图像配置样式（上传预览 + 动作切换 + 状态在左按钮在右），并补充 `/workflow -> /workflow/new` 重定向入口，且模板图像预览已改为通过独立 `template-preview` 路由按 `template_path` GET 回填（重开编辑器不再退化为文件名/路径文本）；Tasks 列表中的工作流摘要已改为显示“总步骤数（排除 start/end）”，不再显示 agent-only 步骤，并新增“日志”按钮与弹窗查看 run 级 `node_results` 明细；会话侧栏设置按钮在收起态保持左下角固定；Skills 缺失用户目录时自动创建 `~/.ghost-os/skills`，Skills 设置页现支持启用/禁用与删除，禁用项对 AI 运行态完全隐藏但仍保留在设置页，列表按启用优先排序；消息区 assistant 文本已支持 Markdown/GFM 渲染与多语言代码块标签展示（轻量无高亮、含纯文本快路径），代码块右上角复制 UI 已收敛为纯图标、无白底无文案，并新增 `assistant_markdown_enabled`、`tool_call_compact_output_enabled` 与 `session_system_prompt_visible_enabled` 运行时开关（关闭后消息区不再显示首条 system prompt），tool 卡片支持会话 artifact 图片预览，tool 详情新增“summary:”可读化摘要并在失败态将 `error:` 固定置顶，文件类 tool 的 summary/compact 现统一展示规范化完整路径，且结构化 `completion_delta tool_call_start/delta/end` 已接通即时预览，工具名一出现即可先显示 pending 卡片；`bash_exec`、`sfind` 与原子文件工具卡片标题现会在 streaming preview/running 与历史回填路径上直接显示 command/load/read/write 等动作，详情区不再重复同一条 summary/step，compact 模式下收敛为成功/失败与必要输出预览；structured tool preview 现会在进入真实 tool lifecycle 时重置上一轮 `tool_call_index` 映射，并为 delayed-id 预览分配唯一 message id，修复多轮工具调用被错误折叠到同一卡片、无法按先后自上而下展示的问题；聊天消息流样式已切到黑白极简版（用户黑底右对齐、assistant 直接正文输出并移除 `// SYSTEM_OUTPUT` 标签与图标、thinking/tool 卡片统一可折叠状态条，thinking 流式分段已并入统一顺序轨道，遇到工具调用会截断当前 thinking 并将后续 thinking 段追加到工具卡片后，回合结束后仍以折叠卡片保留，历史同步与刷新回填时会按下一个实际展示消息锚点保位，保留与 tool 卡片的原始上下顺序，并修复用户消息栈宽度收缩导致的过早换行）；Processing Logic（thinking）现已在 Web 端按会话写入本地持久化并在历史加载/同步时回填，解决刷新后丢失；会话侧栏展开宽度已由 `w-72` 调整为等价 `w-76`（`w-[19rem]`），左侧列表横向空间更宽；设置弹层遮罩模糊由 `backdrop-blur-sm` 下调到 `backdrop-blur-[2px]`，并为左右滚动容器补 `overscroll-contain + touch-pan-y` 以缓解上下滚动迟滞；成功态 tool step 详情背景现与失败态统一为浅白底，避免黑底阅读突兀。
- Web 配置：设置页新增独立 `编排` tab 与 `/?settings=orchestration` 深链；当前已支持在配置页内直接新建命名编排、列表卡片点击进入 `/orchestration/[id]` 独立画布编辑，并复用 task workflow 画布 UI 做前端本地持久化，显式文案已切到编排语义；工作流/编排新建仍默认带 `start/end`，但节点库已移除手动添加入口，且这两个默认边界节点不可复制/删除。
- Web 配置：提示词库空状态文案已去除内层卡片容器（圆角/边框/背景），仅保留外层设置卡片，避免二层嵌套视觉；提示词库主列表已移除内层“库”容器与重复二级标题，卡片直接外露展示；提示词库“新建卡片”弹窗现与“编辑卡片”按模式区分（新建标题/主按钮文案独立，新建态不显示删除，编辑态不再提供重置）；库卡片“激活/编辑/删除”按钮已强制单行，`insert_point=memory` 在库页统一显示为字面量 `memory`；聊天输入框左下角模型选择现仅显示当前激活 provider 组。
## 当前约束
- Native 与 GUI executor 仍在快速演进，不按生产级承诺；memory mode 当前仅保留配置开关与日记忆文件自动建档，不再向 system prompt 注入 Memory 段，后续以稳定性验证为主。
- RSS `system_action`（`RSS_INBOX_POLL`、`RSS_BRIEFING_BUILD`）已从任务契约与启动编排移除，运行时仅保留用户任务类型。
- 当前优先级：稳定性、可观测性、契约一致性；不做无边界扩功能。
- 详细历史请查 `git log`；本文件仅保留阶段摘要。
