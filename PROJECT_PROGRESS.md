# Ghost-OS 项目进展（摘要版）
更新日期：2026-05-10
当前阶段：MVP 稳定化（主链路可用，持续收口；会话未分类分区固定置顶）
文档规则：仅保留阶段结论，`总行数 <= 30`。
## 总览
- 三层架构稳定：Execution（`drivers/native`）/ Central（`core/bridge`）/ Perception（`apps/web`、`apps/cli`、`apps/android`）。
- 当前主开发中心为 `core/bridge`，目标是稳定性、可观测性、契约一致性。
- Assistant 工具调用基线：`<t:ID>JSON</t>` + `[TOOL_TAG_RESULT]`。
- 协作基线：可并行即优先多代理；跨进程交互统一 `trace_id` 可追踪。

## 分层状态
- Central：会话、工具调度、SSE、任务、配置主链路可用；`ServiceResult` 契约已落地并随 bus/API/tool schema contracts 下沉到 `orchestration/internal/contracts`，transport 仅负责 HTTP 映射；`tools/contracts` 已去除对 `session.Session` 的直接依赖；工具执行器已补调用前 `ctx.Done` 快速失败与 tool_call 参数摘要的调试开关守卫，且工具失败结果在 finish event 因流断开失败时仍会随完整 tool-call 回合提交，避免孤儿 tool_call；流契约已扩展 `completion_delta.kind=thinking` 并覆盖 codex/anthropic/openai(custom) reasoning 流转，OpenAI 兼容 provider 在工具往返后现会透传 assistant `reasoning_content` 以支持 thinking 模式续跑，长会话续聊所需上下文窗口现优先走单供应商配置里的 `context_window_tokens`，且会话热窗口加载、请求前裁剪与 `microcompact` 现都会保留完整 reasoning/tool user-turn、避免把 thinking replay 链路从中间裁坏；会话历史构建现会在请求前清理孤儿 `tool` / 损坏 tool-call span，避免旧坏 session 因未知 `tool_call_id` 卡死续聊；agent 失败回合不再提前提交用户消息到主历史，而流式失败/取消仍会由 orchestration 补存本轮 user + assistant draft；空消息续跑现仅允许明确的人类回答恢复场景，避免误触发无定义 resume 把历史写乱；本地 reasoning replay 校验现已覆盖 tool-loop 结束后的最终 assistant 文本，避免 DeepSeek 类 thinking provider 把缺失 `reasoning_content` 的坏历史落盘并在下一轮续聊时报 400，且流式失败回合现在也会保留本轮 user message 与 assistant draft 落盘；runtime system prompt 现会复用同轮技能发现结果，并为 `SKILL.md` 发现缓存与 system prompt 文件读取增加文件签名失效，避免重复读盘同时确保提示词刷新能感知技能/提示词文件变更；runtime execution client 已抽出最小 `runtime.Client`/`Response` 契约并保留 `trace_id` 调用形状；`tool_call_started/finished` 现新增 `arguments_json/output` 以便前端即时可读化展示；orchestration 顶层已删除不可达 pro runner/parser，并将 run registry、session push、stream checkpoint、turn draft 与 run transcript 投影下沉到 `internal/app/trace`、`internal/domain/sessionturn`、`internal/domain/task`，顶层保留兼容薄层；复杂函数与超长文件持续拆分中。
- Perception：Web/CLI 已可用并接通流式与用户图片输入；Web 聊天状态和 API 解析链路已收敛并补测，本批继续将 chat stream/store 纯 reducer 与 message tool/detail view-model 下沉到 `lib/chat-*`，config/workflow 组件侧 API 调用收口到 `hooks/*`，组件边界已由 `rg` 验收并补充 ESLint 固化；新增 streaming thinking 面板（正文首 token 自动折叠、可手动展开/收起、结束清理），tool 卡片现会在工具成功但 agent loop 继续时继续保留参数标题（不再退回成工具名），流式 terminal error 在拿到 `session_id` 后也会立即回读最近历史，避免失败回合看起来整段消失；General 现可配置非 CLI 默认 `project_root`，CLI 则会在每次 agent 请求里显式上送启动目录作为请求级覆盖，消息区虚拟列表行测量已改为稳定 ref 以避免 `measureElement` 更新循环，消息区滚动跟随现已拆到独立 hook/布局签名状态机，streaming thinking/assistant/tool 增量仅在回到底部阈值内时自动跟随，并通过 virtualizer rAF ResizeObserver 合并与禁用 `overflow-anchor` 减少抖动；Web 代理现可从 `GHOST_CONFIG_PATH`/`~/.ghost-os/config.toml` 回读 `api_token`，仓库同时提供 `systemd --user` 开机自启脚本、unit 模板与图形会话环境导入脚本；Android 已接入会话与展示基础，API 契约模型现由 `gen-contracts` 从 schema 生成 Kotlinx Serialization data class，成熟度低于 Web/CLI。
- Central/Web：任务总超时已改为运行时可配置 `task_execution_timeout_ms`（默认 300000ms / 5 分钟），接通 bridge 配置存储、公开配置接口、TaskScheduler 热更新与 Web General 设置页；接力任务已改为任务级 `agent_message` 配置，Web 侧现以“循环”卡片页管理，支持总任务、AI 自停/最大轮次、预设挂载与 `0` 无总超时，AI 自停可提前完成但仍配置最大轮次作为强制截断，用户最大轮次策略会跑满轮次除非手动停止；relay 工具 schema 已改为结构化生成并补测，修复循环首轮 request marshal 因非法 `json.RawMessage` 失败；任务页现排除循环任务，仅保留普通文本与工作流任务，普通文本任务编辑器也支持挂载预设并同步严格工具 allowlist。
- Execution：截图、输入、脚本、窗口查询等原子能力可用；`CODEX_CLI` 已回归 native 原子 action；Wayland 截图已接入 ScreenCast+PipeWire 持续会话（`OpenPipeWireRemote` + `pipewiresrc` 单帧抓取），减少识别链路反复截图造成的暗屏；bridge/native 启动与首次截图日志现在会显式输出 persistent 配置、session 类型与 capture backend，便于定位“代码已改但实际仍跑在 X11”这类问题；native 关键路径测试与 CI 矩阵已补齐，`task.py` 的 `ping/agent/serve/build` 现在默认以 `python-sandbox` 特性编译 native，并通过 `GHOST_NATIVE_BINARY_PATH_OVERRIDE` 显式覆盖文件配置里的 `native_binary_path`，避免脚本链路误回退到旧 release native 导致 `SCRIPT_EXEC unavailable` 或沙箱能力不一致，但整体仍未生产完备。
## 当前可用能力
- 会话：创建、流式输出、持久化、恢复、分页历史、草稿态展示；新增 `session_title_mode` 新会话标题策略，标题持久化在 `state_json` 并由 Web 优先展示本地 alias / 后端 title / 默认 id；删除会话时会同步删除人类可读 markdown 日志（含孤儿日志清理）；用户 stop 会等待 run 退出并保留中断 assistant 草稿，Web 随后回读最近历史。
- 工具：`list_files`、`read_file`、`search_files`、`write_file`、`apply_diff`、`bash_exec`、`script_exec`、`screen_action`、`screen_control`、`web_search`、`sfind` 可运行，`codex_cli` 默认不再覆盖本机 Codex 的 model/sandbox 配置，resume 参数已适配 Codex CLI 0.130.0，fork 交互式限制已显式暴露，旧 `full_auto` 兼容映射为 `sandbox=workspace-write`，完成态现从 JSONL 日志显式回传最后 `agent_message` 为 `final_message` 并供 microcompact 优先摘要，且本次拆分后 `script_exec` 行为保持不变；`sfind` 已取代 `tfind` 并收敛为 skill-only 动态加载器，仅搜索/加载/卸载/列出 `SKILL.md` skill，不再动态加载 tools 或 skill 依赖 tools；runtime prompt 现新增静态 `Skills:` 目录与动态 `Skill Context:` 正文区，并与 `sfind` 共用同一套可见 skill 解析（支持 `skill_blocklist`、repo/user 同名去重、repo 优先），同一 run 内 `sfind(load/unload)` 后下一次 completion 会自动刷新 system prompt，Web tool detail/compact step 现会直接显示 `sfind` 的 `load/unload/list/search` 动作与 skill 名称，不再退化为通用 `run`；`script_exec` 公开脚本 API 已硬切为裸函数 `list_files/read_file/search_files/write_file/apply_diff/bash_exec/fetch_webpage`，不再公开 `tools` 对象，旧 `tools.*` 仅在 Web 历史日志摘要解析中保留展示兼容；顶层 helper 现兼容位置参数/命名参数，且 prompt/description 已补充 `list_files` 返回字符串数组这一真实契约，`write_file` 现会自动创建缺失父目录，`bash_exec` 现支持显式 `max_output_chars` 覆盖默认 stdout 截断预算；native sandbox 现额外提供受限 text-only `open(path, mode)` shim（仅 `r/w/a` + UTF-8，其他模式显式报错），并预注入 `json/os/sys`、将 `subprocess.run/check_output` 与 `os.popen/os.system` 收敛为走受控 shell shim；原 system `Tool Guidance` 已下沉到各工具默认 prompt，并会在加载时自动迁移旧默认 prompt 文件；`/api/prompts/system` 预览中的 `tool_definitions` 与真实 `CompletionRequest.Tools` 现已统一收敛到同一套简化 description/schema；`web_rooter` 与旧任务管理工具已从 bridge 工具目录、运行时注册、提示引导、共享契约与 Web 运行时设置链路中移除；`text_input` 已合并进 `screen_control action=text_input` 并从独立工具目录移除；`read_and_summarize` 已从运行时与契约移除；`screen_control` 现通过独立无 `cwd` execution client 运行，避免切换或失效 `project_root` 时 `find_icon`/截图预览被 `SCREEN_CAPTURE chdir` 误伤；`screen_action` 已为 OCR/icon 链路增加短 TTL 截图复用（`reuse_cache`/`cache_ttl_ms`），降低高频识别场景重复截图带来的闪屏感。
- 任务：`agent_message`、`workflow` 与新的服务端 `orchestration` 资源均可创建、查询、调度与手动执行；Web bridge 已修复无 body POST 透传，任务“运行”按钮不再误报 `invalid JSON body`；workflow `agent` 节点现支持节点级 `runtime_overrides`（`provider/model/system_prompt/tool_allowlist/max_turns`）并真实透传到 bridge runtime，orchestration v1 现支持 `group` 调度节点、`agent -> group` 成员边、顺序/并行轮次、群主模式（`speaking_mode=owner` + `owner_agent_id`、owner 专用 `orchestration_dispatch`、公开/私密 transcript 隔离、dispatch log，且群主回合现已套用 owner 节点自身 `runtime_overrides`，owner system prompt 会与调度控制提示合并，`private_send` 可向成员投递不同私聊内容并仅注入收件人成员上下文，`end_group` 也会写入 `dispatch_results`）与运行内 member session 复用及 group 级结构化日志（`completed_rounds/member_session_ids/shared_transcript/member_results/dispatch_results`），成员运行时新增 `preset_id`，成员级 `max_turns` 会在前后端 normalize/validate 与运行快照中被剔除，显式 `tool_allowlist` 现作为该成员运行的完整工具覆盖；已补 orchestration 基线回归（sequential / parallel / owner public_once / private_once / private_send / end_group / legacy boundary），起步 `internal/*` 包边界，已将 PlanBuilder、transcript contract、owner dispatch 校验、member runner、round dispatcher、standard group executor、owner dispatch tool、owner decision adapter、dispatch executor、owner group executor、ResultMapper、app Runner 与 agentturn send/stream/stop usecase 下沉到 internal 分层；Web editor 分层迁移已完成本批收口：workflow draft reducer 保持在 `lib/workflow-editor` 且控制器显式生成 ID，workflow/orchestration controller hooks 已迁入 `hooks/*` 并拆出 lifecycle/actions/save/preset 等编排 helper，orchestration 草稿快照与语义 facade 下沉到 `lib/orchestration-editor`，编排侧不再暴露 session import no-op 合同，并新增最小 import 边界 lint；前端解析已兼容历史 run 的 `node_results` 缺失/`null`；workflow/orchestration 运行现生成 run 级展示会话，节点/成员/工具与 if/loop/轮次/调度事件按分割线展示，前端侧栏会识别 node_results 中的 workflow agent 与 orchestration owner/member 执行 session，避免其继续落入未分类。
- Agent：支持后端 `mode=plan`（仅输出编排文本，禁止工具与任务执行）；聊天入口已移除 `pro` 文本触发，普通聊天始终走标准单次 agent，接力只由任务配置启用。
- Workflow：`if/loop` 运行时与画布编辑可用；工具节点按 `input_schema` 固定参数，通用 workflow 变量现仅恢复 `${find_icon}` 一条外部引用，输入 `{` 时只展示该候选，运行时可解析 `${find_icon}` 与手写 `${find_icon.x}/${find_icon.y}`，同时仍保留 `screen_control workflow_steps` 内的专用 `coordinate_ref="${find_icon}"`；支持节点复制与连线编辑；`screen_control` 已收敛为 atomic-only，屏幕编排动作已移除 `ocr_scan`、将 `click_text` 收敛为 `find_text`、并支持 `click` 坐标编辑后直连原子点击；`find_icon` 支持模板上传与即时识别预览，预览请求默认 `max_results=1`，native 匹配在 single-result 路径并行化+提前剪枝，缓解“检测中”长时间卡住；`hover_after_match` 现在会在前端测试与工作流运行时都透传为真实 `MOUSE_MOVE` 悬停，若 native 产物过旧则显式提示重编 `drivers/native`；运行时仍支持 `workflow_template_data_url` 自动落盘并输出 `exists/match_count/primary_match` 便于流程判断；Workflow `screen_control` 现新增 `workflow_steps`（多步顺序执行、失败即停、与 `action/params` 共存时报错，默认在相邻步骤间加入 `200~300ms` 随机延迟），前端编排器已改为 0/1/多步分支同步并支持从任务定义回填步骤队列，且 `click` 步骤现可直接引用同工具内最近一次 `find_icon` 的返回坐标；画布 loop start/end 语义已收敛为“end 仅保留退出边”，不再要求显式 `end -> start` 回边；agent 运行时归一化现已做成幂等更新，避免 `WorkflowEditorClient` 在新建/旧草稿恢复场景下触发最大更新深度循环。
- Web 配置：General / Tasks / Skills / Tools / Presets 页面可用；设置导航已移除“偏好”分组并新增“提示词”分组；提示词设置已拆分为“库 / 预览”双页（兼容 `settings=prompts` 自动落到库页），继续复用独立 `/api/prompts/system` 接口，并新增独立 `/api/presets` CRUD/activate：预设按严格 `tool_allowlist` + `prompt_refs(rule/core_job/memory/context[])` 管理、仅持久化提示词卡 ID 引用，现支持一键激活并同步当前严格工具 allowlist、`tool_allowlist_only` 与提示词卡激活顺序；库页已改为提示词卡片列表 + 点击弹层编辑（`name`/`insert_point`/`content`/`active`），新建/编辑时插入点现收敛为 `rule/core_job/context`，其中 `rule/core_job` 继续单激活、`context` 支持多张同时激活并按保存顺序渲染，legacy `memory` 卡片仅保留兼容显示且不再插入 system prompt，Preset 编辑器也已支持按勾选顺序绑定多张 context 卡片；默认系统提示词已收敛为单版本扁平模板（`Role/Job/Skills/Skill Context/Context`），后端持久化 `prompt_library` 并由激活 `core_job` 卡片反向编译 `core_prompt`，预览页现以单一只读块连续展示完整渲染提示词与当前生效工具定义 JSON，且“库 / 预览”已保持与其他设置页一致的左右宽度；`global_template` / `tool_prompt` / `tool_key_spec` 已从 system prompt 存储、API 契约与运行时链路移除，legacy prompt 文件会在加载时自动清理，且已补 UI/Hook/API/parser 测试；Tools 仍只负责每个工具的 prompt 覆盖与启停；会话侧栏“历史分组开关”在开关两种模式下均统一按最近活跃时间降序展示（分区顺序保持不变，分区内忽略拖拽顺序，拖拽仅改变分区归属）；分区右键菜单已支持“新建/重命名/删除”（删除后会话回收到未分类）；会话分区已改为后端持久化，旧 `localStorage` 仅用于一次性迁移，刷新页面或重开 Web/bridge 后仍保留归属；会话侧栏现会从任务/工作流/编排运行日志提取 session 归属并置入不可重命名/删除的系统分区，且系统分区内会按具体任务/工作流/编排资源自动生成只读子分区；General 运行时设置现已改为字段变更防抖热更新并移除底部“保存/重新加载”按钮，页首“界面语言 / 历史会话分组”也已统一切到与其余运行时项一致的连线式 section，新增 `microcompact_enabled`、`max_turns`、`llm_completion_retry_count` 与 `llm_completion_retry_interval_ms` 热更新项，其中 LLM completion 瞬时错误与 thinking/reasoning replay 协议错误现已共享同一套可配置自动重试并持久化到 `config.toml`；`chat_path` 清空后会正确回退 provider 默认路径、`/api/config` 同步返回覆盖值语义，General 不再暴露供应商 `API Key/base_url`，供应商页现可直接编辑单供应商 `context_window_tokens`，编辑时留空 `API Key` 会保留已保存值；运行时设置中的 GraphQL 文本工具调用协议相关开关与配置链路已移除；Workflow 工具参数编辑对 `screen_control` 隐藏 `mode` 与顶层 `display_id`，保留必要输入键，`click` 步骤编辑弹窗已支持“获取鼠标位置”取点（轮询当前坐标、Enter 确认、Esc 取消，并回写 `display_id` 以对齐原子点击缩放），相对坐标模式现会在运行时先读取当前鼠标位置再叠加偏移，避免误用屏幕绝对坐标；屏幕编排步骤的追加/移动/删除/编辑现会统一同步到 `screen_control` 参数，删除 `click` 步骤后不再执行遗留 `click_icon`；`find_icon` 步骤编辑弹窗已切换为新版图像配置样式（上传预览 + 动作切换 + 状态在左按钮在右），并补充 `/workflow -> /workflow/new` 重定向入口，且模板图像预览已改为通过独立 `template-preview` 路由按 `template_path` GET 回填（重开编辑器不再退化为文件名/路径文本）；Tasks 列表中的工作流摘要已改为显示“总步骤数（排除 start/end）”，不再显示 agent-only 步骤，并新增“日志”按钮与弹窗查看 run 级 `node_results` 明细；会话侧栏设置按钮在收起态保持左下角固定；Skills 缺失用户目录时自动创建 `~/.ghost-os/skills`，Skills 设置页现支持启用/禁用与删除，禁用项对 AI 运行态完全隐藏但仍保留在设置页，列表按启用优先排序；消息区 assistant 文本已支持 Markdown/GFM 渲染与多语言代码块标签展示（轻量无高亮、含纯文本快路径），已移除 `assistant_html_enabled` 安全 HTML 子集渲染开关与 raw HTML 渲染链路，代码块右上角复制 UI 已收敛为纯图标、无白底无文案，并新增 `assistant_markdown_enabled`、`tool_call_compact_output_enabled` 与 `session_system_prompt_visible_enabled` 运行时开关（关闭后消息区不再显示首条 system prompt），assistant 文本后续仍有 tool 卡片时复制按钮现会贴正文右侧显示，tool 卡片支持会话 artifact 图片预览，tool 详情新增“summary:”可读化摘要并在失败态将 `error:` 固定置顶，文件类 tool 的 summary/compact 现统一展示规范化完整路径，且结构化 `completion_delta tool_call_start/delta/end` 已接通即时预览，工具名一出现即可先显示 pending 卡片；`bash_exec`、`sfind` 与原子文件工具卡片标题现会在 streaming preview/running 与历史回填路径上直接显示 command/load/read/write 等动作，详情区不再重复同一条 summary/step，compact 模式下收敛为成功/失败与必要输出预览；structured tool preview 现会在进入真实 tool lifecycle 时重置上一轮 `tool_call_index` 映射，并为 delayed-id 预览分配唯一 message id，修复多轮工具调用被错误折叠到同一卡片、无法按先后自上而下展示的问题；聊天消息流样式已切到黑白极简版（用户消息浅灰背板右对齐且长文本默认保留三行、用右上角透明倒三角展开/收起、assistant 直接正文输出并移除 `// SYSTEM_OUTPUT` 标签与图标、thinking/tool 卡片统一可折叠状态条，thinking 流式分段已并入统一顺序轨道，遇到工具调用会截断当前 thinking 并将后续 thinking 段追加到工具卡片后，回合结束后仍以折叠卡片保留，历史同步与刷新回填时会按下一个实际展示消息锚点保位，保留与 tool 卡片的原始上下顺序，并修复用户消息栈宽度收缩导致的过早换行）；Processing Logic（thinking）现已切到后端会话历史 `sessionMessage.thinking` + 最新窗口 `turn_draft` 恢复，已移除 Web 端 localStorage thinking 回填与锚点合并，刷新不再丢失或错位；会话侧栏展开宽度已由 `w-72` 调整为等价 `w-76`（`w-[19rem]`），左侧列表横向空间更宽；设置弹层遮罩模糊由 `backdrop-blur-sm` 下调到 `backdrop-blur-[2px]`，并为左右滚动容器补 `overscroll-contain + touch-pan-y` 以缓解上下滚动迟滞；成功态 tool step 详情背景现与失败态统一为浅白底，避免黑底阅读突兀。
- Web 配置：设置页新增独立 `编排` tab 与 `/?settings=orchestration` 深链；当前已支持在配置页内直接新建命名编排、通过独立 `/api/orchestrations` 列表/运行/日志接口进入 `/orchestration/[id]` 画布编辑，并在首次进入设置时把旧 `ghost.orchestration.*` 本地草稿迁移为服务端资源；编排设置列表现已补回启用/禁用按钮与状态 badge，继续复用独立 `/api/orchestrations` 更新接口，并将卡片操作区对齐任务列表布局、补上删除按钮与确认链路；编排 HTTP 路由向 bridge 转发时的 task `scope` 现已显式透传，修复 `POST/GET /api/orchestrations*` 误报 `task not found`；`/workflow` 与 `/orchestration` 的 `agent` 节点现共用可编辑运行时配置（provider/model/system prompt/tools/max turns），其中 orchestration 成员卡片现支持挂载 preset、挂载时同步 preset 工具 allowlist 并禁用手填 system prompt，成员工具列表改为显示全部已配置工具名且不再受全局 enabled 视图过滤，旧草稿/历史定义会自动清理成员级 `max_turns`；orchestration 侧新增 `group` 节点编辑、独立校验与 group 轮次日志展示，群组 `shared_context` 编辑区现已按“群主初始提示词”呈现并为新建群组预填默认群聊提示；workflow 新建仍默认带 `start/end`，orchestration 已移除 `start/end` 边界节点并改为从首个 group 直接开始执行，旧定义加载/运行时会自动兼容并忽略旧边界节点，节点库仍移除手动添加入口。
- Web 配置：编排设置列表的“运行”按钮现已补最小可见反馈，点击后仅当前编排卡片会切到 `运行中...`，请求结束后自动恢复；若该编排日志弹窗已打开，运行结束后会自动刷新当前日志列表，避免手动运行后界面看起来像“没反应”或“没有新日志”。
- Web 配置：提示词库空状态文案已去除内层卡片容器（圆角/边框/背景），仅保留外层设置卡片，避免二层嵌套视觉；提示词库主列表已移除内层“库”容器与重复二级标题，卡片直接外露展示；提示词库“新建卡片”弹窗现与“编辑卡片”按模式区分（新建标题/主按钮文案独立，新建态不显示删除，编辑态不再提供重置）；Preset 新建/编辑弹窗已切到浅色圆角 UI，工具 allowlist 改为胶囊按钮，rule/Core Job/context 继续复用原保存契约；库卡片“激活/编辑/删除”按钮已强制单行，`insert_point=memory` 在库页统一显示为字面量 `memory`；聊天输入框左下角模型选择现仅显示当前激活 provider 组，且不再受 `tool_allowlist_only` / Preset 激活影响；聊天主面板顶部已新增会话刘海，复用侧栏重命名 alias 显示当前会话名与完整 id；Web 关闭/退出 X 按钮已统一复用设置弹层样式。
- Web 配置：空工具 Preset 激活现允许 `tool_allowlist_only=true` + 空 `tool_allowlist`，表示严格禁止工具调用，工具列表 schema 构建不再因此失败；system prompt 预览无工具时仍返回 `tool_definitions: []`，避免前端严格解析报 expected array；主页设置弹层表单下拉现统一为圆润灰阶 Dropdown 样式，聊天模型选择器仍保持独立样式，模型选择弹窗已加宽并禁止横向滚动。
## 当前约束
- Native 与 GUI executor 仍在快速演进，不按生产级承诺；memory mode 当前仅保留配置开关与日记忆文件自动建档，不再向 system prompt 注入 Memory 段，后续以稳定性验证为主。
- RSS `system_action`（`RSS_INBOX_POLL`、`RSS_BRIEFING_BUILD`）已从任务契约与启动编排移除，运行时仅保留用户任务类型。
- 当前优先级：稳定性、可观测性、契约一致性；契约生成校验与层级守卫已接入 `task.py`；新增 `docs/audit-sandbox-in-driver.md` 完成 native sandbox 越层审计；`docs/orchestration-map.md` 已更新为 orchestration 顶层 169 文件五域盘点，pro dead-candidate 已清理且 `tfind` 摘要文件已更名为 `sfind`；不做无边界扩功能。详细历史请查 `git log`；本文件仅保留阶段摘要。
