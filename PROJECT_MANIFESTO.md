**Ghost-OS** 不是一个传统的远程桌面工具，而是一个**受 AI 驱动的“数字孪生执行层”**。它的目标是让用户通过 Web 或 CLI，像操作自己的双手一样，指挥 AI 在远程计算机上完成任何复杂任务。

## 2. 核心架构哲学 (Core Philosophy)
*   **极致轻量 (Minimalist):** 拒绝任何臃肿的二进制包。底层必须是原生的、高性能的（Rust/Go）。
*   **代码优先 (Bash-First):** 默认系统操作应通过脚本（Bash/PowerShell/Python）完成。只有在面对无法脚本化的 GUI 时，才开启“视觉补位”。
*   **生态联动 (Seamless Ecosystem):** 浏览器（Zen Browser）、终端（CLI）和后端（Go Server）不是孤立的工具，而是一个神经网络的触角。
*   **工程美学 (Clean & Aesthetic):** 代码要像诗一样整洁，UI 要像黑客帝国一样酷炫（Next.js Dark Mode）。

## 3. 三位一体的职能定义 (The Trinity)
开发 AI 在协助编写代码时，必须严格区分以下三个层级的职责：

1.  **执行层 (Rust - The Muscle):**
    *   **定位：** 无状态、原子化。
    *   **任务：** 截图、模拟输入、获取窗口树。它不思考，只负责精准执行。
2.  **中枢层 (Go - The Nervous System):**
    *   **定位：** 状态管理、协议分发、AI 编排。
    *   **任务：** 维护对话上下文、调用 LLM 接口、安全审查、连接各个客户端。
3.  **感知层 (Next.js/Zen/CLI - The Senses):**
    *   **定位：** 交互与反馈。
    *   **任务：** 通过 Web 呈现操作画面，通过魔改浏览器获取网页结构，通过 CLI 提供沉浸式控制。

## 4. AI Agent 决策优先级 (Decision Priority)
当 AI 接收到指令时，必须按以下顺序思考实现路径：
1.  **Level 1 (Scripting):** 优先尝试编写一段本地脚本解决问题。
2.  **Level 2 (API/Native):** 尝试调用系统底层或浏览器的原生接口。
3.  **Level 3 (Vision):** 最后手段——截图、识别、模拟点击。

## 5. 通信原则 (The Golden Rule)
*   所有组件通过 **标准化的消息管道** (Bus) 耦合。
*   严禁组件间出现交叉引用。
*   所有操作必须具备 **可回溯性**（Traceable Log）。

## 6. 开发审美准则
*   **只写必要的代码：** 如果一个功能可以用 10 行解决，绝不写 11 行。
*   **显式胜于隐式：** 逻辑必须清晰可见，严禁过度封装。
*   **默认暗黑模式：** 无论是 Web UI 还是终端输出，追求高对比度的极客审美。兼容写入
