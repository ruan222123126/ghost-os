[package]
name = "native"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
xcap = "0.0.10"
enigo = "0.1.2"
tokio = { version = "1.0", features = ["full"] }
